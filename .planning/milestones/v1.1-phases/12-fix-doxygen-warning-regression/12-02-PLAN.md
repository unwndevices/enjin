---
phase: 12-fix-doxygen-warning-regression
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - include/enjin2/components/probe.hpp
  - include/enjin2/components/planet.hpp
  - include/enjin2/compat/types.hpp
  - include/enjin2/scripting/lua_engine.hpp
  - include/enjin2/components/animation.hpp
  - include/enjin2/ui/system.hpp
  - include/enjin2/scripting/lua_interpreter.hpp
  - include/enjin2/components/image_cache.hpp
  - include/enjin2/core/scene_state_machine.hpp
  - include/enjin2/core/scene.hpp
  - include/enjin2/utils/noise.hpp
  - include/enjin2/utils/polar.hpp
autonomous: true
requirements:
  - DOC-01
  - DOC-03

must_haves:
  truths:
    - "All @return void mismatches in lua_engine.hpp and lua_interpreter.hpp are removed"
    - "All @param name mismatches in lua_engine.hpp pushArg/pushArgs are fixed"
    - "Remaining ~20 header files have essential-level Doxygen documentation"
    - "Doxygen warning count is under 20"
  artifacts:
    - path: "include/enjin2/scripting/lua_engine.hpp"
      provides: "Fixed @return void and @param mismatches"
      contains: "@brief"
    - path: "include/enjin2/scripting/lua_interpreter.hpp"
      provides: "Fixed @return void mismatches"
      contains: "@brief"
    - path: "include/enjin2/compat/types.hpp"
      provides: "Vector3 operators and typedefs documented"
      contains: "@brief"
  key_links:
    - from: "include/enjin2/scripting/lua_engine.hpp"
      to: "include/enjin2/scripting/lua_interpreter.hpp"
      via: "Shared documentation patterns for Lua scripting"
      pattern: "@brief"
---

<objective>
Fix documentation style issues (DOC-03) and document remaining ~20 header files to bring total Doxygen warnings under 20 (DOC-01).

Purpose: Addresses the remaining ~114 warnings not covered by Plan 01, plus fixes the 15 style-specific warnings (@return void mismatches, @param name mismatches) that are the core of DOC-03.
Output: All remaining header files documented. Final Doxygen warning count under 20.
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-doxygen-warning-regression/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix style mismatches and document scripting/compat/animation files</name>
  <files>
    include/enjin2/scripting/lua_engine.hpp
    include/enjin2/scripting/lua_interpreter.hpp
    include/enjin2/compat/types.hpp
    include/enjin2/components/animation.hpp
  </files>
  <action>
Fix documentation style issues (DOC-03) and document remaining files in this batch:

1. **lua_engine.hpp** (8 warnings) — TWO types of fixes:
   - **Remove `@return void`**: Find all `@return void` lines in docblocks for void functions and delete them. There are approximately 3 instances (e.g., setGlobal methods). See 12-RESEARCH.md "Fix: Remove @return void" example.
   - **Fix @param mismatches**: The `pushArg` template function documents `@param args` but signature has `arg`. Change to `@param arg`. The `pushArgs` variadic template documents `first`/`rest` but Doxygen sees single-arg overload. Either add separate docblocks per overload or document only the base case with `@param arg`. See 12-RESEARCH.md "Fix: Template Param Mismatch" example.
   - Add `@brief`, `@param`, `@return` to any remaining undocumented public methods.

2. **lua_interpreter.hpp** (6 warnings) — Remove all `@return void` lines from void function docblocks (approximately 6 instances). Add any missing `@param` documentation.

3. **compat/types.hpp** (9 warnings) — Document Vector3 operators (`operator+`, `operator-`, etc.) with `/// @brief ...` comments. Document namespace enjin typedefs with `/// @brief ...`. These are compatibility wrappers — keep descriptions minimal.

4. **components/animation.hpp** (8 warnings) — Add missing `@return` to non-void functions and `@param` to methods with missing parameter docs. Document undocumented typedefs (PositionTrack, FloatTrack, ColorTrack) with `/// @brief ...`.
  </action>
  <verify>Run `cd /home/unwn/dev/enjin && doxygen docs/Doxyfile 2>/dev/null` then check:
- `grep '@return' doxygen-warnings.log` shows zero "documented return type that does not return anything" warnings
- `grep '@param' doxygen-warnings.log` shows zero "@param is not found" warnings for lua_engine.hpp
- `grep 'lua_engine\|lua_interpreter\|compat/types\|animation' doxygen-warnings.log | grep -c 'warning:'` shows 0</verify>
  <done>All @return void mismatches removed. All @param name mismatches fixed. Compat types and animation typedefs documented.</done>
</task>

<task type="auto">
  <name>Task 2: Document remaining files and verify final warning count</name>
  <files>
    include/enjin2/components/probe.hpp
    include/enjin2/components/planet.hpp
    include/enjin2/ui/system.hpp
    include/enjin2/components/image_cache.hpp
    include/enjin2/core/scene_state_machine.hpp
    include/enjin2/core/scene.hpp
    include/enjin2/utils/noise.hpp
    include/enjin2/utils/polar.hpp
  </files>
  <action>
Document all remaining files that have Doxygen warnings:

1. **components/probe.hpp** (10 warnings) — Add `@param` to methods with missing parameter docs.

2. **components/planet.hpp** (10 warnings) — Add `@param` to methods with missing parameter docs.

3. **ui/system.hpp** (6 warnings) — Add `@brief` and `@param`/`@return` to undocumented functions.

4. **components/image_cache.hpp** (6 warnings) — Document FileInterface methods with `@brief`, `@param`, `@return`.

5. **core/scene_state_machine.hpp** (5 warnings) — Add `@param` to methods with missing parameter docs.

6. **core/scene.hpp** (5 warnings) — Document signal connect methods with `@brief`, `@param`.

7. **utils/noise.hpp** (1-4 warnings) — Document FASTFLOOR, LERP, FADE macros with `/** @brief ... */` and any undocumented functions.

8. **utils/polar.hpp** (1-4 warnings) — Document PI macro with `/** @brief ... */` and any undocumented functions.

**After documenting all files**, check the remaining warnings log for any files not in the original research list (files with 1-2 warnings that were grouped under "Remaining 10 files"). Fix any additional warnings found until total is under 20.

**Style rules:** Essential-level only. No `@return void`. Concise one-line `@brief` descriptions. No verbose examples.

**Final verification:** Run Doxygen and confirm total warning count is under 20.
  </action>
  <verify>Run `cd /home/unwn/dev/enjin && doxygen docs/Doxyfile 2>/dev/null && grep -c 'warning:' doxygen-warnings.log`. Warning count MUST be under 20. If over 20, inspect remaining warnings with `cat doxygen-warnings.log` and fix them.</verify>
  <done>Doxygen warning count is under 20. All files in research priority list are documented. No @return void or @param mismatch warnings remain.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /home/unwn/dev/enjin && doxygen docs/Doxyfile 2>/dev/null && grep -c 'warning:' doxygen-warnings.log` — MUST be under 20
2. `grep 'does not return anything' doxygen-warnings.log` — MUST show 0 matches (DOC-03: no @return void mismatches)
3. `grep 'is not found in the argument list' doxygen-warnings.log` — MUST show 0 matches (DOC-03: no @param mismatches)
4. The project still compiles: `cd /home/unwn/dev/enjin && cmake --build build --target enjin2 2>&1 | tail -1` shows success
</verification>

<success_criteria>
- Doxygen warning count under 20 (DOC-01 satisfied)
- Zero @return void mismatches (DOC-03 satisfied)
- Zero @param name mismatches (DOC-03 satisfied)
- All remaining ~20 header files have essential-level documentation
- No compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-doxygen-warning-regression/12-02-SUMMARY.md`
</output>
