---
phase: 12-fix-doxygen-warning-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/enjin2/graphics/canvas.hpp
  - include/enjin2/graphics/canvas_esp32s3.hpp
  - include/enjin2/ui/components.hpp
  - include/enjin2/graphics/sprite.hpp
  - include/enjin2/components/drawable.hpp
  - include/enjin2/components/satellite.hpp
  - include/enjin2/graphics/canvas_extended.hpp
  - include/enjin2/core/types.hpp
  - include/enjin2/graphics/primitives.hpp
  - include/enjin2/ui/component.hpp
  - include/enjin2/abstract/icanvas.hpp
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "Top 10 warning files have essential-level Doxygen documentation on all public members"
    - "PixelType typedef documented in ICanvas base class (resolves 7 inherited warnings)"
    - "Canvas8 class has @brief and @tparam documentation"
    - "All typedefs in canvas.hpp and primitives.hpp have @brief comments"
  artifacts:
    - path: "include/enjin2/graphics/canvas.hpp"
      provides: "Canvas8 class docs, typedef docs, method docs"
      contains: "@brief"
    - path: "include/enjin2/abstract/icanvas.hpp"
      provides: "PixelType typedef documentation"
      contains: "@brief"
    - path: "include/enjin2/graphics/canvas_esp32s3.hpp"
      provides: "Method param/return docs"
      contains: "@param"
  key_links:
    - from: "include/enjin2/abstract/icanvas.hpp"
      to: "include/enjin2/graphics/canvas.hpp"
      via: "PixelType typedef inheritance"
      pattern: "PixelType"
---

<objective>
Document the top 10 highest-warning header files to eliminate ~190 Doxygen warnings.

Purpose: These 10 files account for approximately 63% of all 304 warnings. Fixing them brings the total well toward the <20 threshold.
Output: Documented header files with essential-level Doxygen comments (@brief, @param, @return).
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-doxygen-warning-regression/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document graphics module high-priority files</name>
  <files>
    include/enjin2/graphics/canvas.hpp
    include/enjin2/graphics/canvas_esp32s3.hpp
    include/enjin2/graphics/sprite.hpp
    include/enjin2/graphics/canvas_extended.hpp
    include/enjin2/graphics/primitives.hpp
    include/enjin2/abstract/icanvas.hpp
  </files>
  <action>
Add essential-level Doxygen documentation to resolve warnings in the 6 graphics-related files. Work file by file:

1. **icanvas.hpp** — Document the `PixelType` typedef: `/// @brief Pixel type used by this canvas`. This resolves 7 inherited warnings across derived classes.

2. **canvas.hpp** (56 warnings) — Add:
   - `@brief` + `@tparam WIDTH` + `@tparam HEIGHT` to the `Canvas8` class declaration (currently only has a plain comment)
   - `/** @brief ... */` to all undocumented typedefs (Canvas8_128x64, etc.)
   - `@param` and `@return` to all undocumented public methods
   - Do NOT add `@return` to void methods

3. **canvas_esp32s3.hpp** (24 warnings) — Add `@param` and `@return` to all undocumented public methods. Follow same essential-level pattern.

4. **sprite.hpp** (18 warnings) — Add `@brief` to undocumented functions, `@param`/`@return` where missing.

5. **canvas_extended.hpp** (13 warnings) — Add `@brief`, `@param`, `@return` to undocumented functions.

6. **primitives.hpp** (11 warnings) — Add `@brief` to undocumented typedefs and `@param` to functions with missing params.

**Style rules (per Phase 9 decisions):**
- One sentence per `@brief`
- Practical, concise descriptions
- NO `@return void` on void functions
- NO verbose examples or multi-paragraph descriptions
  </action>
  <verify>Run `cd /home/unwn/dev/enjin && doxygen docs/Doxyfile 2>/dev/null` then `grep -c 'warning:' doxygen-warnings.log` — count should be significantly lower than 304. Check `grep 'canvas\|sprite\|primitives\|icanvas\|canvas_extended\|canvas_esp32s3' doxygen-warnings.log | grep -c 'warning:'` shows fewer than 5 warnings from these files.</verify>
  <done>All 6 graphics files have essential-level documentation. No Doxygen warnings referencing Canvas8 as undocumented class. PixelType typedef documented in base class.</done>
</task>

<task type="auto">
  <name>Task 2: Document UI, components, and core high-priority files</name>
  <files>
    include/enjin2/ui/components.hpp
    include/enjin2/components/drawable.hpp
    include/enjin2/components/satellite.hpp
    include/enjin2/core/types.hpp
    include/enjin2/ui/component.hpp
  </files>
  <action>
Add essential-level Doxygen documentation to resolve warnings in 5 non-graphics files:

1. **ui/components.hpp** (19 warnings) — Add `@brief` to undocumented functions, `@param`/`@return` where missing.

2. **components/drawable.hpp** (18 warnings) — Document PascalCase setter/getter methods. These are compatibility wrappers — use brief descriptions like `/// @brief Set the position` / `/// @brief Get the position`.

3. **components/satellite.hpp** (15 warnings) — Add `@param` and `@return` to methods with missing parameter documentation.

4. **core/types.hpp** (12 warnings) — Add `@brief` to undocumented members, structs, and typedefs.

5. **ui/component.hpp** (10 warnings) — Add `@brief` to undocumented functions, `@param`/`@return` where missing.

**Style rules:** Same essential-level style as Task 1. No `@return void` on void functions. Concise one-line @brief descriptions.
  </action>
  <verify>Run `cd /home/unwn/dev/enjin && doxygen docs/Doxyfile 2>/dev/null` then `grep 'components\|drawable\|satellite\|types\|component\.hpp' doxygen-warnings.log | grep -c 'warning:'` shows fewer than 5 warnings from these files.</verify>
  <done>All 5 files have essential-level documentation on public members. PascalCase wrappers in drawable.hpp documented.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `cd /home/unwn/dev/enjin && doxygen docs/Doxyfile 2>/dev/null && grep -c 'warning:' doxygen-warnings.log`
2. Warning count should be approximately 110-130 (304 minus ~190 from these files)
3. No warnings mentioning Canvas8 as undocumented compound
4. No warnings for PixelType typedef
</verification>

<success_criteria>
- All 11 files (including icanvas.hpp) have essential-level Doxygen documentation
- Warning count reduced by approximately 190 (from 304)
- No new warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-doxygen-warning-regression/12-01-SUMMARY.md`
</output>
