---
phase: 15-cleanup-ci-and-readme-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/docs.yml
  - scripts/deploy-docs.sh
  - README.md
autonomous: true
requirements:
  - TECH-DEBT

must_haves:
  truths:
    - "generate-api-docs.js runs exactly once during CI pipeline (via CMake docs target only)"
    - "generate-api-docs.js runs exactly once during local deploy script (via CMake docs target only)"
    - "README license badge shows MIT (not TBD)"
    - "README license section says MIT License (not placeholder text)"
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "CI pipeline without duplicate generate-api-docs.js step"
      contains: "cmake --build . --target docs"
    - path: "scripts/deploy-docs.sh"
      provides: "Deploy script without duplicate generate-api-docs.js call"
    - path: "README.md"
      provides: "License badge and section with MIT"
      contains: "license-MIT"
  key_links:
    - from: ".github/workflows/docs.yml"
      to: "CMakeLists.txt"
      via: "cmake --build . --target docs runs generate-api-docs.js"
      pattern: "cmake --build.*--target docs"
    - from: "scripts/deploy-docs.sh"
      to: "CMakeLists.txt"
      via: "cmake --build . --target docs runs generate-api-docs.js"
      pattern: "cmake --build.*--target docs"
---

<objective>
Remove duplicate generate-api-docs.js invocations from CI pipeline and deploy script, and replace README license TBD placeholder with MIT.

Purpose: Close the final two tech debt items from the v1.1 milestone audit.
Output: Clean CI pipeline, correct license metadata in README.
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cleanup-ci-and-readme-tech-debt/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate generate-api-docs.js from CI and deploy script</name>
  <files>.github/workflows/docs.yml, scripts/deploy-docs.sh</files>
  <action>
In `.github/workflows/docs.yml`: Remove the "Generate API documentation" step entirely (lines 76-78). This is the step with `name: Generate API documentation` and `run: node scripts/generate-api-docs.js`. The CMake `docs` target at line 53 (`cmake --build . --target docs`) already runs generate-api-docs.js as its second command. The "Build Docusaurus site" step should directly follow the "Check Doxygen warning count" step.

In `scripts/deploy-docs.sh`: Remove lines 17-18 (`echo "Generating API documentation..."` and `node scripts/generate-api-docs.js`). The `cmake --build . --target docs` at line 14 already handles this. The script should go directly from `cd ..` to `echo "Building Docusaurus site..."`.
  </action>
  <verify>
    <automated>grep -c "generate-api-docs" /home/unwn/dev/enjin/.github/workflows/docs.yml /home/unwn/dev/enjin/scripts/deploy-docs.sh | grep -v ":0$" | wc -l | grep -q "^0$" && echo "PASS: no generate-api-docs references in CI or deploy script" || echo "FAIL: generate-api-docs still referenced"</automated>
    <manual>Verify docs.yml flows: checkout -> setup -> install -> doxygen install -> generate doxygen XML (cmake docs target) -> check warnings -> build docusaurus -> deploy. No separate generate-api-docs step.</manual>
  </verify>
  <done>generate-api-docs.js is invoked exactly zero times explicitly in docs.yml and deploy-docs.sh (only via CMake docs target)</done>
</task>

<task type="auto">
  <name>Task 2: Replace README license TBD with MIT</name>
  <files>README.md</files>
  <action>
In `README.md` line 3: Change the license badge from:
`![License](https://img.shields.io/badge/license-TBD-green)`
to:
`![License](https://img.shields.io/badge/license-MIT-green)`

In `README.md` lines 106-108: Change the license section from:
```
## License

[Add license information]
```
to:
```
## License

MIT License
```

Source of truth: `library.json` line 17 declares `"license": "MIT"`.
  </action>
  <verify>
    <automated>grep -q "license-MIT" /home/unwn/dev/enjin/README.md && grep -q "MIT License" /home/unwn/dev/enjin/README.md && echo "PASS" || echo "FAIL"</automated>
    <manual>README badge shows MIT, license section says MIT License.</manual>
  </verify>
  <done>README license badge shows MIT and license section contains "MIT License"</done>
</task>

</tasks>

<verification>
1. `grep -r "generate-api-docs" .github/workflows/docs.yml scripts/deploy-docs.sh` returns no results
2. `grep "license-MIT" README.md` returns the badge line
3. `grep "MIT License" README.md` returns the license section
4. CMakeLists.txt still contains `node.*generate-api-docs.js` in the docs target (not removed from wrong location)
</verification>

<success_criteria>
- generate-api-docs.js runs only once in CI pipeline (via cmake docs target)
- generate-api-docs.js runs only once in local deploy script (via cmake docs target)
- README license badge shows MIT
- README license section says MIT License
</success_criteria>

<output>
After completion, create `.planning/phases/15-cleanup-ci-and-readme-tech-debt/15-01-SUMMARY.md`
</output>
