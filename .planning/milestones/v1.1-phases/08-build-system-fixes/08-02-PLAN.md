---
phase: 08-build-system-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "User can find complete list of dependencies in README"
    - "User can see which dependencies are required vs optional"
    - "User can find installation instructions for Lua"
  artifacts:
    - path: "README.md"
      provides: "Dependencies documentation"
      contains: "## Dependencies"
  key_links:
    - from: "README.md"
      to: "CMakeLists.txt"
      via: "ENJIN2_BUILD_LUA option reference"
      pattern: "ENJIN2_BUILD_LUA"
---

<objective>
Document all external dependencies in README.md with Required vs Optional categorization. Provide installation instructions for Lua and the ENJIN2_BUILD_LUA CMake option.

Purpose: Users need to know what dependencies are required to build enjin2 and how to install them. Lua is optional (ENJIN2_BUILD_LUA), so users should understand when they need it and how to install it.
Output: Updated README.md with Dependencies section containing clear categorization and installation instructions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-build-system-fixes/08-CONTEXT.md
@.planning/phases/08-build-system-fixes/08-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Add Dependencies section to README</name>
  <files>README.md</files>
  <action>
    Add a Dependencies section to README.md after the Features section (before Project Structure):

    ```markdown
    ## Dependencies

    enjin2 includes all required functionality as single-header vendor libraries.

    ### Required

    None - all core functionality is self-contained for basic usage.

    ### Optional

    - **Lua** (version 5.1+): Scripting support for game logic.
      - Install from: https://lua.org/
      - Package managers:
        - Debian/Ubuntu: `apt-get install liblua5.1-dev`
        - macOS: `brew install lua`
      - Build without Lua: Set `ENJIN2_BUILD_LUA=OFF` when running CMake
      - Used by: `C_LuaScript` component, scripting subsystem

    ### Vendor Libraries

    enjin2 includes these single-header libraries (no installation required):

    - **Adafruit GFX Library**: Graphics font rendering and text display
      - Location: External library at `../Libs/Adafruit-GFX-Library`
      - Used by: UI widgets, text rendering

    - **stb_image_write.h**: BMP image writing
      - Location: `vendor/` directory
      - Used by: Canvas export functionality

    ```

    Why: Users need to know what dependencies are required and how to install them. Lua is optional, so users building without scripting support don't need to install it. Vendor libraries are included in the repo, so they don't require installation.
  </action>
  <verify>
    Read README.md and verify:
    - "## Dependencies" section exists
    - "### Required" section exists and lists "None"
    - "### Optional" section exists with Lua details
    - "### Vendor Libraries" section exists with Adafruit GFX and stb_image_write
    - Lua section includes installation instructions and ENJIN2_BUILD_LUA option
  </verify>
  <done>
    README.md contains a complete Dependencies section with Required, Optional, and Vendor Libraries subsections.
  </done>
</task>

<task type="auto">
  <name>Update Installation section with Lua option</name>
  <files>README.md</files>
  <action>
    Update the Installation section in README.md to mention the ENJIN2_BUILD_LUA option:

    Find this section:
    ```markdown
    ## Installation

    ```bash
    git clone https://github.com/unwndevices/enjin.git
    cd enjin
    mkdir build && cd build
    cmake ..
    cmake --build .
    ```
    ```

    Replace with:
    ```markdown
    ## Installation

    ### Basic build
    ```bash
    git clone https://github.com/unwndevices/enjin.git
    cd enjin
    mkdir build && cd build
    cmake ..
    cmake --build .
    ```

    ### Build without Lua (docs deployment)
    ```bash
    mkdir build && cd build
    cmake -DENJIN2_BUILD_LUA=OFF ..
    cmake --build .
    ```
    ```

    Why: Users need to know how to build without Lua (e.g., for docs deployment or when they don't need scripting). The ENJIN2_BUILD_LUA option is the primary way to control this.
  </action>
  <verify>
    Read README.md Installation section and verify:
    - Basic build command is present
    - Build without Lua section is present with `-DENJIN2_BUILD_LUA=OFF` flag
    - Both sections are formatted correctly
  </verify>
  <done>
    Installation section includes both basic build and build without Lua commands.
  </done>
</task>

</tasks>

<verification>
1. README.md has "## Dependencies" section
2. Dependencies section has "### Required", "### Optional", and "### Vendor Libraries" subsections
3. Lua is listed under Optional with installation instructions
4. ENJIN2_BUILD_LUA option is documented
5. Installation section shows how to build without Lua
6. Adafruit GFX and stb_image_write are listed as vendor libraries
</verification>

<success_criteria>
- Users can find complete list of dependencies in README
- Users understand which dependencies are required vs optional
- Users know how to install Lua if needed
- Users know how to build without Lua
</success_criteria>

<output>
After completion, create `.planning/phases/08-build-system-fixes/08-02-SUMMARY.md`
</output>
