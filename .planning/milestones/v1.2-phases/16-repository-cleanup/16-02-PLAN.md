---
phase: 16-repository-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/latex/
autonomous: true
requirements:
  - REPO-01
  - REPO-02

must_haves:
  truths:
    - "docs/latex/ is not tracked by git"
    - "docs/latex/ is listed in .gitignore"
    - "Future Doxygen runs do not re-add LaTeX files to git tracking"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion rule for docs/latex/"
      contains: "docs/latex/"
  key_links:
    - from: ".gitignore"
      to: "docs/latex/"
      via: "gitignore pattern"
      pattern: "docs/latex/"
---

<objective>
Remove generated LaTeX files from git tracking and prevent future re-addition via .gitignore.

Purpose: Generated artifacts should not be version-controlled — they bloat the repo and create noisy diffs.
Output: docs/latex/ untracked by git, .gitignore updated.
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Untrack LaTeX files and update .gitignore</name>
  <files>
    .gitignore
    docs/latex/
  </files>
  <action>
    Step 1: Add `docs/latex/` to `.gitignore`. Place it in the "Generated documentation" section alongside the existing `docs/xml/` entry. The result should look like:

    ```
    # Generated documentation
    docs/xml/
    docs/latex/
    ```

    Step 2: Remove docs/latex/ from git tracking without deleting the files from disk:
    ```bash
    git rm -r --cached docs/latex/
    ```

    This preserves the files locally (Doxygen may regenerate them) but removes them from version control.

    Step 3: Stage the .gitignore change:
    ```bash
    git add .gitignore
    ```

    Step 4: Verify the files are no longer tracked:
    ```bash
    git status docs/latex/
    ```
    Should show nothing (untracked files are hidden by .gitignore).
  </action>
  <verify>
    <automated>cd /home/unwn/dev/enjin && git ls-files docs/latex/ | wc -l | grep -q '^0$' && grep -q 'docs/latex/' .gitignore && echo "PASS"</automated>
  </verify>
  <done>docs/latex/ is not tracked by git. .gitignore contains docs/latex/ entry. Running `git status` shows no latex files as tracked or untracked.</done>
</task>

</tasks>

<verification>
1. `git ls-files docs/latex/` returns empty (no tracked files)
2. `.gitignore` contains `docs/latex/` line
3. `git status` does not show any docs/latex/ files
</verification>

<success_criteria>
- Zero LaTeX files tracked by git
- .gitignore has docs/latex/ entry
- Future Doxygen LaTeX output will be ignored by git
</success_criteria>

<output>
After completion, create `.planning/phases/16-repository-cleanup/16-02-SUMMARY.md`
</output>
