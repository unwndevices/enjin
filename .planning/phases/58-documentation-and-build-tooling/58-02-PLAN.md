---
phase: 58-documentation-and-build-tooling
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - docs/src/tutorials/your-first-script.md
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "A reader new to this engine understands the engine.* API concepts (state, input, graphics, time) after reading the tutorial"
    - "Tutorial uses excerpts from tamagotchi.lua, not a line-by-line walkthrough"
    - "All Lua code blocks render with syntax highlighting"
    - "Tutorial page appears in the Tutorials sidebar category"
  artifacts:
    - path: "docs/src/tutorials/your-first-script.md"
      provides: "Your First Script tutorial"
      contains: "engine.state"
  key_links:
    - from: "docs/sidebars.js"
      to: "docs/src/tutorials/your-first-script.md"
      via: "doc ID tutorials/your-first-script"
      pattern: "tutorials/your-first-script"
---

<objective>
Write the "Your First Script" tutorial — a concept-first guide introducing the engine's Lua scripting model to developers who know game dev basics but are new to enjin2. Uses tamagotchi.lua excerpts to illustrate each concept.

Purpose: DOC-02 — first tutorial onboarding a new user to write a working Lua script for this engine.

Output: docs/src/tutorials/your-first-script.md
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-documentation-and-build-tooling/58-CONTEXT.md
@.planning/phases/58-documentation-and-build-tooling/58-RESEARCH.md
@.planning/phases/58-documentation-and-build-tooling/58-01-SUMMARY.md

<interfaces>
<!-- Key Lua API facts for tutorial content. Verified from codebase source. -->

From scripts/tamagotchi.lua (210 lines):

Script structure (mandatory globals):
```lua
function update(dt)
    -- dt: delta time in seconds (clamped to 0.05 max)
end

function draw()
    -- no arguments; runs after update each frame
end
```

engine.config:
```lua
local W, H = engine.config.resolution()  -- returns width, height integers
```

engine.state:
```lua
engine.state.switch("alive")      -- transition to named state
local cur = engine.state.current() -- returns current state name as string
```

engine.input:
```lua
engine.input.just_pressed(BTN.START)  -- true on first frame button is down
engine.input.just_pressed(BTN.A)
engine.input.just_pressed(BTN.LEFT)
engine.input.just_pressed(BTN.RIGHT)
-- BTN table: UP, DOWN, LEFT, RIGHT, A, B, START
```

engine.time:
```lua
engine.time.now()  -- float: total seconds since start
```

Drawing globals (tamagotchi.lua line 3 note: "all drawing globals are also available via engine.graphics.*"):
```lua
clear(COLOR.BLACK)
setColor(COLOR.WHITE)
text("HELLO", x, y)
textCentered("CENTERED", x, y)
rectangle(x, y, w, h)
circle(cx, cy, r)
line(x1, y1, x2, y2)
setPixel(x, y)
-- Colors: COLOR.BLACK, COLOR.WHITE, COLOR.GRAY, COLOR.DARK_GRAY,
--         COLOR.RED, COLOR.GREEN, COLOR.BLUE, COLOR.PINK, COLOR.YELLOW
```

Key tamagotchi.lua excerpts to use as illustrations:
- Lines 5: resolution
- Lines 28-36: engine.state.switch in reset()
- Lines 42-43: update(dt) + dt clamping
- Lines 49, 51-55: engine.state.current() branch
- Lines 52, 68, 78, 89, 100: engine.input.just_pressed
- Lines 130-131, 135-136: draw() + clear + setColor
- Line 177: engine.time.now()

SDL3 runner invocation:
```bash
./build/sdl3/enjin2_sdl --script scripts/tamagotchi.lua
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write "Your First Script" tutorial</name>
  <files>docs/src/tutorials/your-first-script.md</files>
  <action>
Create `docs/src/tutorials/your-first-script.md`. This file must NOT exist yet — create a new directory `docs/src/tutorials/` if needed.

**File path:** `docs/src/tutorials/your-first-script.md`
**Doc ID** (used by sidebar): `tutorials/your-first-script`

The tutorial MUST follow the locked decisions from CONTEXT.md:
- Concept-first structure: introduce the engine.* API concepts, then show tamagotchi.lua excerpts to illustrate
- Do NOT walk through tamagotchi.lua line by line
- Target reader: knows game dev basics, new to this engine
- Concise, practical tone: engine API first

**Required structure:**

```markdown
---
title: Your First Script
sidebar_label: Your First Script
---

# Your First Script

[2-3 sentence intro: what Lua scripts are in this engine, and that update/draw are the two hooks the runner calls]

## Script Structure

[Explain: every script defines update(dt) and draw(). Runner calls them each frame. dt is clamped to 0.05.]

Show the mandatory globals with a lua code block. Then reference tamagotchi.lua as a complete working example.

## Reading Resolution

[engine.config.resolution() — why useful: knowing canvas dimensions for layout]

Show the tamagotchi.lua line 5 excerpt.

## State Management

[engine.state: named states as a built-in FSM. switch() transitions, current() reads. Great for alive/dead/sleeping style game states.]

Show tamagotchi.lua lines around reset() and the cur == "dead" check.

## Handling Input

[engine.input.just_pressed() — fires once on the frame the button goes down. The BTN table.]

Show tamagotchi.lua input handling excerpts (two or three examples).

## Drawing

[draw() runs after update. clear() before drawing. setColor() then draw commands. Two valid forms: bare globals (clear, setColor) and engine.graphics.* — both work because the SDL3 runner exposes all drawing globals. Use whichever feels natural.]

Show tamagotchi.lua draw() opening and one drawing sequence.

## Time

[engine.time.now() for animation timing. Use modulo for blinking effects.]

Show tamagotchi.lua line 177 excerpt.

## Running Your Script

[How to launch: ./build/sdl3/enjin2_sdl --script path/to/script.lua]

## Next Steps

[Link to Async Coroutines tutorial for advanced timing patterns]
```

**Code block language:** All Lua examples must use ` ```lua ` code fences (not plain ` ``` ` or ` ```javascript `). The bash example must use ` ```bash `.

**Content guidelines:**
- Each section: 2-4 sentences of explanation + a code excerpt
- Excerpts must be actual tamagotchi.lua lines (not invented code) — quote them verbatim
- Do not use phrases like "we'll now explore" or "let's look at" — state facts directly
- The tutorial is a reference walkthrough, not a tutorial series prologue

**Do NOT include:**
- Engine internals (C++ classes, component system) — this tutorial is purely about Lua scripting
- The async/coroutine API — that belongs in the Async Coroutines tutorial (Plan 03)
- Line-by-line walkthrough of all 210 lines of tamagotchi.lua
  </action>
  <verify>
    <automated>ls /home/unwn/git/enjin/docs/src/tutorials/your-first-script.md && grep -n "engine.state" /home/unwn/git/enjin/docs/src/tutorials/your-first-script.md && grep -n "engine.input" /home/unwn/git/enjin/docs/src/tutorials/your-first-script.md && grep -n "\`\`\`lua" /home/unwn/git/enjin/docs/src/tutorials/your-first-script.md</automated>
  </verify>
  <done>File exists at docs/src/tutorials/your-first-script.md with frontmatter, all five engine.* concepts covered (config, state, input, graphics, time), and at least three lua code fences using tamagotchi.lua excerpts.</done>
</task>

</tasks>

<verification>
- `ls docs/src/tutorials/your-first-script.md` succeeds
- `grep -c '```lua' docs/src/tutorials/your-first-script.md` returns 3 or more
- `grep "engine.state\|engine.input\|engine.config\|engine.time" docs/src/tutorials/your-first-script.md` returns matches for all namespaces
- `grep "Canvas8" docs/src/tutorials/your-first-script.md | wc -l` returns 0 (no stale references)
</verification>

<success_criteria>
Tutorial file exists with correct doc ID path, covers all five engine.* API concepts using tamagotchi.lua excerpts, uses ```lua code fences throughout, and contains no engine internals or async API content.
</success_criteria>

<output>
After completion, create `.planning/phases/58-documentation-and-build-tooling/58-02-SUMMARY.md`
</output>
