---
phase: 01-dependency-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-dependency-analysis/namespace-verification.md
autonomous: true

must_haves:
  truths:
    - "All namespace enjin references documented in verification report"
    - "Three pattern types checked: declarations, usages, using statements"
    - "AST-level analysis completed via clang-tidy"
    - "Test code excluded from analysis"
  artifacts:
    - path: ".planning/phases/01-dependency-analysis/namespace-verification.md"
      provides: "Namespace reference findings and verification results"
      sections: ["## Pattern Analysis", "## AST Analysis", "## Findings"]
  key_links:
    - from: "namespace-verification.md"
      to: "enjin2 source code"
      via: "ripgrep multi-pattern + clang-tidy AST analysis"
      pattern: "(namespace enjin|enjin::|using namespace enjin)"
---

<objective>
Verify no `namespace enjin` references exist in enjin2 codebase using multi-pattern grep and AST-level analysis.

Purpose: Ensure enjin2 has zero enjin1 namespace dependencies before build isolation.
Output: Namespace verification report documenting all findings (should be none).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-analysis/01-dependency-analysis-CONTEXT.md
@.planning/phases/01-dependency-analysis/01-dependency-analysis-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Run multi-pattern grep for namespace references</name>
  <files>
    .planning/phases/01-dependency-analysis/namespace-verification.md
  </files>
  <action>
    1. Scan enjin2 codebase for namespace enjin patterns using ripgrep:
       - Pattern 1: Namespace declarations: `rg "namespace\s+enjin\b" enjin2/`
       - Pattern 2: Namespace usages: `rg "\benjin::" enjin2/`
       - Pattern 3: Using statements: `rg "using\s+namespace\s+enjin\s*;" enjin2/`

    2. Exclude test directories from all scans:
       - Add glob exclusions: `--glob='!test/**' --glob='!tests/**' --glob='!examples/**'`
       - Only analyze production code

    3. Capture results with file paths and line numbers:
       - For each pattern match: file_path, line_number, matched_text, pattern_type
       - Example: `enjin2/engine.cpp:42: enjin::ComponentManager::update() [usage]`

    4. Create verification report at .planning/phases/01-dependency-analysis/namespace-verification.md:

       ```markdown
       # Namespace Verification Report

       ## Pattern Analysis

       ### Namespace Declarations (`namespace enjin`)
       - **Total matches**: 0
       - **Files scanned**: X
       - **Results**: No namespace enjin declarations found ✓

       ### Namespace Usages (`enjin::`)
       - **Total matches**: 0
       - **Files scanned**: X
       - **Results**: No enjin:: usages found ✓

       ### Using Statements (`using namespace enjin;`)
       - **Total matches**: 0
       - **Files scanned**: X
       - **Results**: No using namespace enjin; statements found ✓

       ## AST Analysis

       [Will be populated by clang-tidy task]

       ## Findings

       ### Summary
       - **Total namespace references found**: 0
       - **Test files excluded**: Yes
       - **Verification status**: PASS

       ### Detailed Results
       [If matches found, list them here with file:line context]
       ```

    5. If any matches found:
       - List each match with file:line and full context line
       - Categorize by pattern type (declaration, usage, using)
       - Flag for cleanup before build isolation
  </action>
  <verify>
    1. Report exists: `test -f .planning/phases/01-dependency-analysis/namespace-verification.md`
    2. Has pattern analysis sections: `grep -E "^### (Namespace Declarations|Namespace Usages|Using Statements)" namespace-verification.md`
    3. Verify grep was run with exclusions (check if we ran rg with --glob flags in command history)
  </verify>
  <done>
    namespace-verification.md contains Pattern Analysis section with three pattern types (declarations, usages, using statements), total match counts, and detailed results. Test code excluded.
  </done>
</task>

<task type="auto">
  <name>Run clang-tidy for AST-level namespace analysis</name>
  <files>
    .planning/phases/01-dependency-analysis/namespace-verification.md
  </files>
  <action>
    1. Ensure compile_commands.json exists:
       - Check if already present: `test -f build/compile_commands.json`
       - If not, regenerate: `cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -B build`

    2. Run clang-tidy on enjin2 source files to detect hidden namespace references:
       - Command: `clang-tidy enjin2/**/*.cpp -checks="-*,modernize-use-override" 2>&1 | rg -i "enjin"`
       - Alternative: Use readability checks that catch namespace contamination
       - Capture all output containing "enjin" references

    3. Analyze clang-tidy output for:
       - AST-level namespace references not caught by pattern matching
       - Using declarations bringing enjin symbols into scope
       - Type aliases or typedefs referencing enjin types
       - Template instantiations using enjin types

    4. Update namespace-verification.md with AST Analysis section:

       ```markdown
       ## AST Analysis

       ### Clang-Tidy Scan
       - **Tool**: clang-tidy
       **Scope**: enjin2/**/*.cpp (excluding tests)
       - **Total files analyzed**: X

       ### Results
       - **AST-level references found**: 0
       - **Using declarations**: 0
       - **Type aliases**: 0
       - **Template references**: 0

       [If issues found, list with file:line and description]
       ```

    5. Cross-reference with pattern analysis:
       - Check if clang-tidy found issues not caught by grep patterns
       - Note any discrepancies in findings section
  </action>
  <verify>
    1. AST Analysis section exists: `grep -E "^## AST Analysis" namespace-verification.md`
    2. Clang-tidy was executed: Check tool availability and execution
    3. compile_commands.json exists or was generated
  </verify>
  <done>
    namespace-verification.md includes AST Analysis section with clang-tidy results, showing tool, scope, and findings summary.
  </done>
</task>

<task type="auto">
  <name>Generate findings summary and recommendations</name>
  <files>
    .planning/phases/01-dependency-analysis/namespace-verification.md
  </files>
  <action>
    1. Synthesize findings from both analyses:
       - Combine pattern analysis results with AST analysis results
       - Calculate total namespace references across all detection methods
       - Determine verification status: PASS (0 references) or FAIL (references found)

    2. Update Findings section in namespace-verification.md:

       ```markdown
       ## Findings

       ### Summary
       - **Total namespace references found**: 0
       - **Test files excluded**: Yes
       - **Verification status**: PASS ✓

       ### Detection Methods Used
       1. Pattern matching (ripgrep):
          - Namespace declarations: 0 matches
          - Namespace usages (enjin::): 0 matches
          - Using statements: 0 matches

       2. AST analysis (clang-tidy):
          - AST-level references: 0
          - Using declarations: 0
          - Type aliases: 0

       ### Recommendations
       [If PASS]
       - ✓ No namespace enjin references detected in enjin2 codebase
       - ✓ Ready to proceed with build target isolation

       [If FAIL - list cleanup steps]
       - Remove namespace declarations found in files X, Y, Z
       - Replace enjin:: usages with enjin2:: equivalents in files A, B
       - Remove using namespace enjin; statements and qualify names
       - Re-run verification after cleanup
       ```

    3. Ensure report is complete and actionable:
       - Clear PASS/FAIL status
       - If FAIL: Provide specific file:line locations for cleanup
       - If PASS: Confirm readiness for build isolation
  </action>
  <verify>
  1. Findings section has summary: `grep -E "^### Summary" namespace-verification.md`
  2. Status is clear (PASS or FAIL): `grep -E "status: (PASS|FAIL)" namespace-verification.md`
  3. Recommendations provided: `grep -E "^### Recommendations" namespace-verification.md`
  </verify>
  <done>
    namespace-verification.md contains complete Findings section with summary, detection methods breakdown, and clear recommendations based on verification status.
  </done>
</task>

</tasks>

<verification>
Run validation to ensure namespace verification completeness:
- Confirm all three pattern types were checked (declarations, usages, using statements)
- Verify clang-tidy AST analysis was completed
- Check that test code was excluded from analysis
- Ensure report has clear PASS/FAIL status with actionable recommendations
</verification>

<success_criteria>
- namespace-verification.md exists with Pattern Analysis, AST Analysis, and Findings sections
- Three pattern types checked: namespace declarations, enjin:: usages, using statements
- AST-level analysis completed via clang-tidy
- Test code excluded from all analysis
- Clear PASS/FAIL status with recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-analysis/01-02-SUMMARY.md`
</output>
