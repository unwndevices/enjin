---
phase: 17-documentation-generation-fix
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - docs/api/abstract/*.md
  - docs/api/animation/*.md
  - docs/api/components/*.md
  - docs/api/core/*.md
  - docs/api/effects/*.md
  - docs/api/graphics/*.md
  - docs/api/scripting/*.md
  - docs/api/ui/*.md
  - docs/api/utils/*.md
autonomous: true
requirements: [DOCG-01, DOCG-02, DOCG-03, DOCG-04]

must_haves:
  truths:
    - "Method signatures on API pages show single const where appropriate, not const const"
    - "Cross-reference text in documentation renders as readable, properly-spaced strings"
    - "extractText() output does not include xml2js $ attribute object content"
    - "All API markdown files are regenerated and the documentation site builds without errors"
  artifacts:
    - path: "docs/api/graphics/Sprite.md"
      provides: "Primary verification target — had both garbling and const const issues"
      contains: "Sprite class for bitmap image rendering"
    - path: "docs/api/*/README.md"
      provides: "Module overview pages regenerated"
  key_links:
    - from: "scripts/generate-api-docs.js"
      to: "docs/api/**/*.md"
      via: "regeneration output"
      pattern: "Generated.*api documentation files"
    - from: "docs/api/**/*.md"
      to: "Docusaurus build"
      via: "npx docusaurus build"
      pattern: "build.*success"
---

<objective>
Regenerate all API markdown files using the fixed generate-api-docs.js and verify the documentation site builds without errors.

Purpose: Code fixes in Plan 01 are meaningless until the generated markdown is updated. This plan produces the actual clean documentation and verifies end-to-end correctness.
Output: ~85 regenerated API pages with clean text, verified by Docusaurus build.
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-documentation-generation-fix/17-RESEARCH.md
@.planning/phases/17-documentation-generation-fix/17-01-SUMMARY.md
@scripts/generate-api-docs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate all API documentation and verify content quality</name>
  <files>docs/api/**/*.md</files>
  <action>
**1. Regenerate all API pages:**
```bash
cd /home/unwn/dev/enjin && node scripts/generate-api-docs.js
```

This should produce ~85 generated files (classes + namespaces + module overviews) across 9 module directories (abstract, animation, components, core, effects, graphics, scripting, ui, utils). The `compat` module should NOT appear since the config entry was removed in Plan 01.

**2. Verify key content corrections:**

Check Sprite.md for both fixes:
- Brief description should read: `Sprite class for bitmap image rendering (matches original Enjin Sprite).` (not fused "SpriteSprite" or garbled ordering)
- Method signatures like `GetTexture()` should show single `const`, not `const const`

Spot-check 2-3 other files with cross-references to confirm ordering is correct.

**3. Verify no compat directory was created:**
```bash
test ! -d docs/api/compat && echo "OK: no compat directory"
```

**4. Verify no regression — no `$` attribute content in output:**
```bash
grep -rl 'refid\|kindref\|classenjin2_1_1' docs/api/ | head -5
```
Should return no matches (these are xml2js `$` attribute values that should never appear in rendered text). Note: `classenjin2` appearing in a Doxygen cross-reference link target is acceptable; what we're checking is that it doesn't appear as visible text content.
  </action>
  <verify>
    <automated>cd /home/unwn/dev/enjin && node -e "
const fs = require('fs');
const path = require('path');

let pass = true;

// 1. Check Sprite.md exists and has correct brief
const sprite = fs.readFileSync('docs/api/graphics/Sprite.md', 'utf-8');
if (sprite.includes('SpriteSprite')) { console.log('FAIL: SpriteSprite garbling still present'); pass = false; }
else { console.log('OK: No SpriteSprite garbling'); }

if (sprite.includes('const const')) { console.log('FAIL: const const still present'); pass = false; }
else { console.log('OK: No const const duplication'); }

if (!sprite.includes('Sprite class for bitmap image rendering')) { console.log('FAIL: Brief description not correct'); pass = false; }
else { console.log('OK: Brief description correct'); }

// 2. No compat directory
if (fs.existsSync('docs/api/compat')) { console.log('FAIL: compat directory exists'); pass = false; }
else { console.log('OK: No compat directory'); }

// 3. Count generated files
let count = 0;
const apiDir = 'docs/api';
for (const mod of fs.readdirSync(apiDir)) {
  const modPath = path.join(apiDir, mod);
  if (!fs.statSync(modPath).isDirectory()) continue;
  for (const f of fs.readdirSync(modPath)) {
    if (f.endsWith('.md')) count++;
  }
}
if (count < 80) { console.log('FAIL: Only ' + count + ' API files (expected 80+)'); pass = false; }
else { console.log('OK: ' + count + ' API files generated'); }

process.exit(pass ? 0 : 1);
"</automated>
  </verify>
  <done>All API markdown files regenerated with clean text — no garbled cross-references, no const const duplication, no compat module output</done>
</task>

<task type="auto">
  <name>Task 2: Verify Docusaurus documentation site builds</name>
  <files>docs/api/**/*.md</files>
  <action>
Run the Docusaurus build to verify the documentation site builds without errors:

```bash
cd /home/unwn/dev/enjin && npx docusaurus build --out-dir /tmp/enjin-docs-build 2>&1
```

Use `/tmp` for build output to avoid polluting the repo.

**Expected:** Build completes with zero broken link errors and zero MDX parse errors.

**If build fails with broken links:** Check that slug frontmatter is correct for nested class pages (ComponentQuery::Iterator, ComponentStorage::Iterator, math::TrigLUT). These need `slug: OutputName` in frontmatter to match link targets. This was already handled by Phase 14's slug generation — verify it still works with the new parser options.

**If build fails with MDX parse errors:** Check that escapeForMdx() is still correctly escaping `<` and `>` in template types. The new extractText() should not change how these are handled since formatType() calls extractText() and the result goes through escapeForMdx().

Clean up build output after verification:
```bash
rm -rf /tmp/enjin-docs-build
```
  </action>
  <verify>
    <automated>cd /home/unwn/dev/enjin && npx docusaurus build --out-dir /tmp/enjin-docs-build 2>&1 | tail -5 && rm -rf /tmp/enjin-docs-build</automated>
  </verify>
  <done>Docusaurus build completes successfully with zero broken links and zero MDX errors</done>
</task>

</tasks>

<verification>
1. `docs/api/graphics/Sprite.md` brief description reads correctly (no fused words)
2. No `const const` in any generated API page: `grep -r 'const const' docs/api/ | wc -l` returns 0
3. No compat directory: `test ! -d docs/api/compat`
4. Docusaurus build passes
5. Generated file count >= 80
</verification>

<success_criteria>
1. All ~85 API markdown files regenerated with correct text
2. No "const const" in any method signature
3. Cross-reference text renders as readable, properly-spaced strings
4. No docs/api/compat/ directory exists
5. Docusaurus documentation site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-documentation-generation-fix/17-02-SUMMARY.md`
</output>
