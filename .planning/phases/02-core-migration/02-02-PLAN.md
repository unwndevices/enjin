---
phase: 02-core-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - enjin2/include/enjin2/seams/component_seam.hpp
  - enjin2/include/enjin2/seams/scene_seam.hpp
autonomous: true

must_haves:
  truths:
    - "Strangler Fig seams exist for component and scene boundaries"
    - "Seams support runtime switching between enjin1 (legacy) and enjin2 (new) implementations"
    - "Seams do not include enjin1 headers (maintains isolation)"
  artifacts:
    - path: enjin2/include/enjin2/seams/component_seam.hpp
      provides: Component compatibility boundary
      contains: "ComponentSeam", "Implementation::LEGACY", "switchToNew"
    - path: enjin2/include/enjin2/seams/scene_seam.hpp
      provides: Scene compatibility boundary
      contains: "SceneSeam", "Backend::ENJIN1", "switchToEnjin2"
  key_links:
    - from: enjin2/include/enjin2/seams/component_seam.hpp
      to: enjin2/include/enjin2/core/component.hpp
      via: include directive
      pattern: '#include.*component\.hpp'
    - from: enjin2/include/enjin2/seams/scene_seam.hpp
      to: enjin2/include/enjin2/core/scene.hpp
      via: include directive
      pattern: '#include.*scene\.hpp'
---

<objective>
Implement Strangler Fig pattern seams for component and scene boundaries. These seams allow enjin1 and enjin2 implementations to coexist during incremental migration, with runtime switching capability.

Purpose: Create compatibility boundaries that route operations to either legacy (enjin1) or new (enjin2) implementation. Seams use void* for legacy handles to avoid including enjin1 headers in enjin2 code, maintaining isolation.

Output: Two seam files (component_seam.hpp, scene_seam.hpp) in enjin2/include/enjin2/seams/
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-migration/02-CONTEXT.md
@.planning/phases/02-core-migration/02-RESEARCH.md

@enjin/Components/Component.hpp
@enjin/Object.hpp
@enjin/Scene.hpp
@enjin/SceneStateMachine.hpp
@enjin2/include/enjin2/core/component.hpp
@enjin2/include/enjin2/core/scene.hpp
</context>

<tasks>

<task type="auto">
  <name>Create component seam</name>
  <files>enjin2/include/enjin2/seams/component_seam.hpp</files>
  <action>
Create enjin2/include/enjin2/seams/component_seam.hpp with:
- `#pragma once` guard
- Include enjin2/core/component.hpp
- Namespace `enjin2`
- Class `ComponentSeam` with:
  - Public enum `class Implementation { LEGACY, NEW };`
  - Private members: `Implementation impl;`, `Component* newImpl;`, `void* legacyImpl;`
  - Constructor: `explicit ComponentSeam(Implementation implementation) : impl(implementation), newImpl(nullptr), legacyImpl(nullptr) {}`
  - Public method `void awake()` that routes based on impl (calls newImpl->awake() if NEW and not null)
  - Public method `void start()` that routes based on impl
  - Public method `void update(uint16_t deltaTime)` that routes based on impl
  - Public method `void switchToNew(Component* component)` that sets newImpl and impl = NEW
  - Getter: `Implementation getImplementation() const { return impl; }`

DO NOT include any enjin headers. Use void* for legacyImpl to maintain isolation.
DO NOT implement LEGACY routing (just stub with comments "// Route to legacy implementation (enjin1)")
</action>
  <verify>
Check header compiles: `cd enjin2 && g++ -I include -c include/enjin2/seams/component_seam.hpp -o /dev/null 2>&1 | grep -E "(error|warning)" || echo "No errors/warnings"`
  </verify>
  <done>
File exists with ComponentSeam class, LEGACY/NEW enum, void* legacyImpl, no enjin includes
</done>
</task>

<task type="auto">
  <name>Create scene seam</name>
  <files>enjin2/include/enjin2/seams/scene_seam.hpp</files>
  <action>
Create enjin2/include/enjin2/seams/scene_seam.hpp with:
- `#pragma once` guard
- Include enjin2/core/scene.hpp, enjin2/core/scene_state_machine.hpp
- Namespace `enjin2`
- Class `SceneSeam` with:
  - Public enum `class Backend { ENJIN1, ENJIN2 };`
  - Private members: `Backend currentBackend;`, `SceneStateMachine* enjin2SM;`, `void* enjin1SM;`
  - Constructor: `explicit SceneSeam(Backend backend) : currentBackend(backend), enjin2SM(nullptr), enjin1SM(nullptr) {}`
  - Public method `void initialize()` that routes to enjin2SM->initialize() if ENJIN2 and not null (stub ENJIN1 path)
  - Public method `void update(uint16_t deltaTime)` that routes to enjin2SM->update(deltaTime) if ENJIN2
  - Public method `void switchToEnjin2(SceneStateMachine* newSM)` that sets enjin2SM and currentBackend = ENJIN2
  - Template method `template<typename PixelType> void render(ICanvas<PixelType>& canvas)` that routes to enjin2SM->render(canvas) if ENJIN2
  - Getter: `Backend getBackend() const { return currentBackend; }`

DO NOT include any enjin headers. Use void* for enjin1SM to maintain isolation.
DO NOT implement ENJIN1 routing (just stub with comments "// Route to enjin1")
</action>
  <verify>
Check header compiles: `cd enjin2 && g++ -I include -c include/enjin2/seams/scene_seam.hpp -o /dev/null 2>&1 | grep -E "(error|warning)" || echo "No errors/warnings"`
  </verify>
  <done>
File exists with SceneSeam class, ENJIN1/ENJIN2 enum, void* enjin1SM, no enjin includes
</done>
</task>

</tasks>

<verification>
Verify Strangler Fig seams are correctly implemented:
1. Both seam files exist in enjin2/include/enjin2/seams/
2. Seams use void* for legacy handles (no enjin header includes)
3. Runtime switching methods exist (switchToNew, switchToEnjin2)
4. Compilation test passes
</verification>

<success_criteria>
Both component and scene seams exist with routing logic for new implementation and stubbed legacy paths. Seams maintain enjin2 isolation (no enjin includes) while enabling incremental migration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-migration/02-02-SUMMARY.md`
</output>
