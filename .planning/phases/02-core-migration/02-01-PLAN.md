---
phase: 02-core-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - enjin2/include/enjin2/compat/types.hpp
  - enjin2/include/enjin2/compat/component.hpp
  - enjin2/include/enjin2/compat/scene.hpp
autonomous: true

must_haves:
  truths:
    - "Compatibility headers exist mapping enjin1 types to enjin2 equivalents"
    - "Type aliases compile without errors"
    - "No enjin1 headers are included in enjin2 compatibility headers"
  artifacts:
    - path: enjin2/include/enjin2/compat/types.hpp
      provides: Type aliases (Vector2, Vector3)
      contains: "namespace enjin", "typedef.*Point.*Vector2"
    - path: enjin2/include/enjin2/compat/component.hpp
      provides: Component lifecycle mapping
      contains: "namespace enjin", "Awake", "Start"
    - path: enjin2/include/enjin2/compat/scene.hpp
      provides: Scene lifecycle mapping
      contains: "namespace enjin", "OnCreate", "OnActivate"
  key_links:
    - from: enjin2/include/enjin2/compat/types.hpp
      to: enjin2/include/enjin2/core/types.hpp
      via: include directive
      pattern: '#include.*types\.hpp'
    - from: enjin2/include/enjin2/compat/component.hpp
      to: enjin2/include/enjin2/core/component.hpp
      via: include directive
      pattern: '#include.*component\.hpp'
---

<objective>
Create compatibility headers that alias enjin1 types to enjin2 equivalents. This enables gradual code migration by allowing existing enjin1 code to compile with enjin2 types underneath.

Purpose: Establish thin type aliases (typedefs) that map enjin1 naming conventions to enjin2 implementations without adaptation logic. Headers live in enjin2 source tree under enjin2/compat/ namespace.

Output: Three compatibility header files (types.hpp, component.hpp, scene.hpp) in enjin2/include/enjin2/compat/
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-migration/02-CONTEXT.md
@.planning/phases/02-core-migration/02-RESEARCH.md

@enjin/utils/Types.hpp
@enjin/Components/Component.hpp
@enjin/Scene.hpp
@enjin2/include/enjin2/core/types.hpp
@enjin2/include/enjin2/core/component.hpp
@enjin2/include/enjin2/core/scene.hpp
</context>

<tasks>

<task type="auto">
  <name>Create types compatibility header</name>
  <files>enjin2/include/enjin2/compat/types.hpp</files>
  <action>
Create enjin2/include/enjin2/compat/types.hpp with:
- `#pragma once` guard
- Include enjin2/core/types.hpp
- Namespace `enjin` (NOT enjin2)
- Type alias: `typedef enjin2::Point Vector2;` (enjin1 uses Vector2, enjin2 uses Point)
- Type alias: `typedef enjin2::Size Size;` (same name, just namespace mapping)
- Type alias: `struct Vector3 { float x, y, z; };` (enjin1 has Vector3, enjin2 doesn't - create as direct struct)
- Add comment: "Migration support - deprecated after enjin1 deletion"

DO NOT include any enjin headers (enjin/). Only include enjin2 headers.
</action>
  <verify>
Compile test: `cd enjin2 && mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && cmake --build . --target enjin2 2>&1 | grep -E "(error|warning)" || echo "No errors/warnings"`
  </verify>
  <done>
File exists with namespace enjin, Vector2 typedef to Point, Vector3 struct defined, no enjin includes
</done>
</task>

<task type="auto">
  <name>Create component lifecycle compatibility header</name>
  <files>enjin2/include/enjin2/compat/component.hpp</files>
  <action>
Create enjin2/include/enjin2/compat/component.hpp with:
- `#pragma once` guard
- Include enjin2/core/component.hpp
- Namespace `enjin` (NOT enjin2)
- Type alias: `using Component = enjin2::Component;`
- Inline functions mapping enjin1 PascalCase to enjin2 camelCase:
  - `static inline void Awake(enjin2::Component* comp) { if (comp) comp->awake(); }`
  - `static inline void Start(enjin2::Component* comp) { if (comp) comp->start(); }`
  - `static inline void Update(enjin2::Component* comp, uint16_t dt) { if (comp) comp->update(dt); }`
  - `static inline void LateUpdate(enjin2::Component* comp, uint16_t dt) { if (comp) comp->lateUpdate(dt); }`
- Add comment: "Migration support - deprecated after enjin1 deletion"

DO NOT use virtual function overrides or adapters - these are simple wrapper functions.
</action>
  <verify>
Check header compiles: `cd enjin2 && g++ -I include -c include/enjin2/compat/component.hpp -o /dev/null 2>&1 | grep -E "(error|warning)" || echo "No errors/warnings"`
  </verify>
  <done>
File exists with Awake/Start/Update/LateUpdate wrapper functions, namespace enjin, no enjin includes
</done>
</task>

<task type="auto">
  <name>Create scene lifecycle compatibility header</name>
  <files>enjin2/include/enjin2/compat/scene.hpp</files>
  <action>
Create enjin2/include/enjin2/compat/scene.hpp with:
- `#pragma once` guard
- Include enjin2/core/scene.hpp
- Namespace `enjin` (NOT enjin2)
- Type alias: `using Scene = enjin2::Scene;`
- Inline functions mapping enjin1 methods to enjin2:
  - `static inline void OnCreate(enjin2::Scene* scene) { if (scene) scene->onCreate(); }`
  - `static inline void OnDestroy(enjin2::Scene* scene) { if (scene) scene->onDestroy(); }`
  - `static inline void OnActivate(enjin2::Scene* scene) { if (scene) scene->onActivate(); }`
  - `static inline void OnDeactivate(enjin2::Scene* scene) { if (scene) scene->onDeactivate(); }`
  - `static inline void Update(enjin2::Scene* scene, uint16_t dt) { if (scene) scene->update(dt); }`
- Add comment: "Migration support - deprecated after enjin1 deletion"

DO NOT add ProcessInput() or Draw() - these don't exist in enjin2 Scene.
</action>
  <verify>
Check header compiles: `cd enjin2 && g++ -I include -c include/enjin2/compat/scene.hpp -o /dev/null 2>&1 | grep -E "(error|warning)" || echo "No errors/warnings"`
  </verify>
  <done>
File exists with OnCreate/OnDestroy/OnActivate/OnDeactivate wrapper functions, namespace enjin, no enjin includes
</done>
</task>

</tasks>

<verification>
Verify compatibility headers compile independently:
1. All three headers exist in enjin2/include/enjin2/compat/
2. Headers use namespace `enjin` (NOT `enjin2`)
3. No enjin headers are included (grep -r '#include.*enjin/' enjin2/include/enjin2/compat/)
4. Compilation test passes without errors
</verification>

<success_criteria>
All three compatibility headers exist and compile successfully. enjin1 code can include these headers and use enjin1 naming conventions while calling enjin2 implementations underneath.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-migration/02-01-SUMMARY.md`
</output>
