---
phase: 05-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - enjin2/CMakeLists.txt
  - enjin2/include/enjin2/seams/scene_seam.hpp
  - enjin2/include/enjin2/seams/component_seam.hpp
  - enjin2/tests/shadow_mode_test.cpp
autonomous: true
user_setup: []

must_haves:
  truths:
    - "CMake builds enjin2 without any USE_ENJIN1 option"
    - "All conditional compilation blocks for enjin1 backend removed from code"
    - "Build system compiles cleanly without enjin1-related flags"
    - "No USE_ENJIN1 or USE_ENJIN1_BACKEND references remain in codebase"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Root CMake configuration without enjin1 backend option"
      contains: "no USE_ENJIN1"
    - path: "enjin2/CMakeLists.txt"
      provides: "enjin2 CMake configuration without conditional compile definitions"
      contains: "no USE_ENJIN1_BACKEND"
    - path: "enjin2/include/enjin2/seams/scene_seam.hpp"
      provides: "Scene seam without conditional compilation or backend enums"
      contains: "no USE_ENJIN1_BACKEND, no Backend enum"
    - path: "enjin2/include/enjin2/seams/component_seam.hpp"
      provides: "Component seam without conditional compilation or implementation enums"
      contains: "no USE_ENJIN1_BACKEND, no Implementation enum"
    - path: "enjin2/tests/shadow_mode_test.cpp"
      provides: "Test file without enjin1 backend conditional compilation"
      contains: "no USE_ENJIN1_BACKEND"
  key_links:
    - from: "CMakeLists.txt"
      to: "enjin2 build"
      via: "CMake configuration"
      pattern: "cmake configure without USE_ENJIN1"
    - from: "enjin2/include/enjin2/seams/*.hpp"
      to: "enjin2 compilation"
      via: "Direct implementation without conditionals"
      pattern: "no preprocessor directives for backend selection"
---

<objective>
Remove all conditional compilation and CMake configuration for enjin1 backend selection, completing the migration to enjin2-only builds.

Purpose: The Strangler Fig pattern allowed incremental migration with backend selection, but enjin1 was never integrated. This phase removes all conditional infrastructure (CMake options, compile definitions, preprocessor blocks, deprecated enums) to achieve a clean, simple enjin2-only build system.

Output: Clean CMake configuration and seam files with no enjin1 backend references, ready for production enjin2 builds.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-final-cleanup/05-RESEARCH.md

# Relevant prior work
@.planning/phases/03-feature-support/03-01-SUMMARY.md  # CMake backend selection added
@.planning/phases/03-feature-support/03-03-SUMMARY.md  # Conditional compilation added to seams
</context>

<tasks>

<task type="auto">
  <name>Remove CMake backend selection configuration</name>
  <files>CMakeLists.txt, enjin2/CMakeLists.txt</files>
  <action>
  Remove CMake option and compile definitions for enjin1 backend selection:

  **CMakeLists.txt (root):**
  - Delete lines 9-15 (USE_ENJIN1 option and message block)
  - Result: No backend selection option, clean enjin2-only build

  **enjin2/CMakeLists.txt:**
  - Delete lines 136-140 (USE_ENJIN1_BACKEND compile definitions block)
  - Result: No conditional compile definitions propagated to dependent targets

  Reference: 05-RESEARCH.md "Removing a CMake Option" and "Removing Conditional Compile Definitions" sections
  </action>
  <verify>grep -n "USE_ENJIN1[^_]" CMakeLists.txt enjin2/CMakeLists.txt</verify>
  <done>grep returns exit code 1 (no matches) - no USE_ENJIN1 references in CMake files</done>
</task>

<task type="auto">
  <name>Remove conditional compilation from seam files</name>
  <files>enjin2/include/enjin2/seams/scene_seam.hpp, enjin2/include/enjin2/seams/component_seam.hpp, enjin2/tests/shadow_mode_test.cpp</files>
  <action>
  Remove all conditional compilation blocks and deprecated backend selection infrastructure:

  **enjin2/include/enjin2/seams/scene_seam.hpp:**
  - Delete Backend enum (lines 22-25: ENJIN1, ENJIN2)
  - Delete member variables: currentBackend, enjin1SM (lines 28-30)
  - Keep only: enjin2SM, id, active, initialized
  - Update constructor to remove Backend parameter: explicit SceneSeam(uint32_t sceneId = 0)
  - Update constructor initializer list to remove backend initialization
  - For each of 6 #if USE_ENJIN1_BACKEND blocks (onCreate, onActivate, onDeactivate, onDestroy, onUpdate, onRender):
    - Delete entire #if block including #error directive and commented enjin1 code
    - Keep only the #else branch content
    - Remove #endif
  - Delete deprecated methods: switchToEnjin2() (lines 174-177), getBackend() (lines 184-186)
  - Update onCreate() and onDestroy() to be simple no-ops (empty implementations)
  - Update onActivate() to set active = true (remove Backend::ENJIN2 check)
  - Update onUpdate() to call enjin2SM->update() directly (remove Backend::ENJIN2 check)
  - Update onRender() to call enjin2SM->render() directly (remove Backend::ENJIN2 check)
  - Update class documentation to remove "enjin1 and enjin2 implementations coexist" language

  **enjin2/include/enjin2/seams/component_seam.hpp:**
  - Delete Implementation enum (lines 18-21: LEGACY, NEW)
  - Delete member variables: impl, legacyImpl (lines 24-26)
  - Keep only: newImpl, enabled
  - Update constructor to remove Implementation parameter: explicit ComponentSeam()
  - Update constructor initializer list to remove impl initialization
  - For each of 7 #if USE_ENJIN1_BACKEND blocks (awake, start, update, lateUpdate, onEnable, onDisable, getOwner):
    - Delete entire #if block including #error directive and commented enjin1 code
    - Keep only the #else branch content
    - Remove #endif
  - Delete deprecated methods: switchToNew() (lines 191-194), getImplementation() (lines 201-203)
  - Update all lifecycle methods to call newImpl directly (remove Implementation::NEW checks)
  - Update getOwner() to return newImpl->getOwner() or nullptr directly (remove conditional)
  - Update class documentation to remove "enjin1 and enjin2 implementations coexist" language

  **enjin2/tests/shadow_mode_test.cpp:**
  - Delete lines 117-121 (#if USE_ENJIN1_BACKEND block)
  - Replace with: output_file = "output-enjin2.bmp";
  - Remove comment about checking USE_ENJIN1_BACKEND compile-time macro

  Reference: 05-RESEARCH.md "Cleaning Up Conditional Compilation" sections
  </action>
  <verify>grep -n "USE_ENJIN1_BACKEND\|Backend\|Implementation" enjin2/include/enjin2/seams/*.hpp</verify>
  <done>grep returns exit code 1 (no matches) - no conditional compilation or backend enums in seam files</done>
</task>

<task type="auto">
  <name>Clean build directories and verify cleanup</name>
  <files>build directories (no persistent files modified)</files>
  <action>
  Remove all build directories to clear stale cache entries and verify clean enjin2-only build:

  **Build directory cleanup:**
  - Remove all build directories: rm -rf enjin2/build verify-off/build build verify-on build-test-off build-enjin1 build-enjin2 build_enjin1
  - Create fresh build directory: mkdir -p build
  - Reconfigure build: cd build && cmake .. -DCMAKE_BUILD_TYPE=Release
  - Verify no USE_ENJIN1 or USE_ENJIN1_BACKEND in cache: grep "USE_ENJIN1" CMakeCache.txt (should return nothing)

  **Verification of cleanup:**
  - Search for all remaining enjin1 backend references:
    - grep -rn "USE_ENJIN1" enjin2/ --include="*.hpp" --include="*.cpp" (should return nothing)
    - grep -rn "USE_ENJIN1_BACKEND" enjin2/ --include="*.hpp" --include="*.cpp" (should return nothing)
    - grep -rn "Backend.*ENJIN1\|Implementation.*LEGACY" enjin2/include/enjin2/seams/ (should return nothing)
  - Verify clean compilation: cmake --build build (should succeed without errors)

  Reference: 05-RESEARCH.md "Build Directory Cleanup" and "Pitfall 2: Stale Cache Entries"
  </action>
  <verify>grep -rn "USE_ENJIN1" enjin2/ --include="*.hpp" --include="*.cpp"</verify>
  <done>grep returns exit code 1 (no matches) and cmake --build build succeeds - no enjin1 references remain in codebase, enjin2 builds cleanly</done>
</task>

</tasks>

<verification>
After completing all tasks, verify the phase success criteria:

1. CMake configuration is clean:
   - No USE_ENJIN1 option in CMakeLists.txt
   - No USE_ENJIN1_BACKEND compile definitions in enjin2/CMakeLists.txt

2. Seam files are simplified:
   - No Backend enum in scene_seam.hpp
   - No Implementation enum in component_seam.hpp
   - No #if USE_ENJIN1_BACKEND conditional blocks in any seam files
   - Direct implementation calls without backend checks

3. Build system works:
   - Clean cmake configure without backend options
   - No enjin1-related references in build cache
   - Successful compilation of enjin2

4. Codebase verified clean:
   - Zero USE_ENJIN1 references in production code
   - Zero USE_ENJIN1_BACKEND references in production code
   - Zero enjin1 backend enum members or checks
</verification>

<success_criteria>
1. grep "USE_ENJIN1" CMakeLists.txt enjin2/CMakeLists.txt returns no matches
2. grep "USE_ENJIN1_BACKEND" enjin2/include/enjin2/seams/*.hpp returns no matches
3. grep "USE_ENJIN1" enjin2/ --include="*.hpp" --include="*.cpp" returns no matches
4. cmake .. -DCMAKE_BUILD_TYPE=Release configures successfully
5. cmake --build build completes without errors
6. Requirement FND-10 satisfied: CMakeLists.txt supports clean enjin2-only builds without enjin1 paths or references
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-cleanup/05-01-SUMMARY.md`
</output>
