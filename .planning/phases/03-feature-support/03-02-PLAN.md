---
phase: 03-feature-support
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["enjin2/include/enjin2/abstract/icanvas.hpp", "enjin2/include/enjin2/abstract/icomponent.hpp", "enjin2/include/enjin2/abstract/iscene.hpp"]
autonomous: true

must_haves:
  truths:
    - "ICanvas interface extended with full rendering API (text, shapes, transformations)"
    - "IComponent interface defines component lifecycle methods (awake, start, update, etc.)"
    - "IScene interface defines scene lifecycle methods (onCreate, onUpdate, onRender, etc.)"
    - "All abstraction interfaces have virtual destructors"
    - "All abstraction methods are pure virtual (= 0)"
    - "abstraction/ directory created under enjin2/include/enjin2/"
  artifacts:
    - path: "enjin2/include/enjin2/abstract/icanvas.hpp"
      provides: "Extended canvas abstraction interface"
      min_lines: 50
      contains: "class ICanvas", "virtual ~ICanvas", "virtual void drawText", "virtual void drawLine"
    - path: "enjin2/include/enjin2/abstract/icomponent.hpp"
      provides: "Component abstraction interface"
      min_lines: 30
      contains: "class IComponent", "virtual void awake", "virtual void start", "virtual void update"
    - path: "enjin2/include/enjin2/abstract/iscene.hpp"
      provides: "Scene abstraction interface"
      min_lines: 30
      contains: "class IScene", "virtual void onCreate", "virtual void onUpdate", "virtual void onRender"
  key_links:
    - from: "icanvas.hpp"
      to: "enjin2/include/enjin2/graphics/canvas.hpp"
      via: "Extends existing ICanvas template pattern"
      pattern: "template.*ICanvas"
    - from: "icomponent.hpp"
      to: "enjin2/include/enjin2/core/component.hpp"
      via: "Mirrors enjin2::Component lifecycle methods"
      pattern: "virtual void awake|start|update"
    - from: "iscene.hpp"
      to: "enjin2/include/enjin2/core/scene.hpp"
      via: "Mirrors enjin2::Scene lifecycle methods"
      pattern: "virtual void onCreate|onUpdate|onRender"
---

<objective>
Create abstract interfaces for canvas, component, and scene that both enjin1 and enjin2 can implement.

Purpose: Enable compile-time polymorphism by defining ICanvas, IComponent, and IScene interfaces with virtual destructors and pure virtual methods, following the existing ICanvas template pattern in enjin2.
Output: Three abstraction interface headers in enjin2/include/enjin2/abstract/ directory.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-support/03-CONTEXT.md
@.planning/phases/03-feature-support/03-RESEARCH.md
@.planning/phases/02-core-migration/02-01-SUMMARY.md
@.planning/phases/02-core-migration/02-02-SUMMARY.md
@enjin2/include/enjin2/graphics/canvas.hpp
@enjin2/include/enjin2/core/component.hpp
@enjin2/include/enjin2/core/scene.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extended ICanvas abstraction interface</name>
  <files>enjin2/include/enjin2/abstract/icanvas.hpp</files>
  <action>
    Create enjin2/include/enjin2/abstract/icanvas.hpp:
    - Forward declare or include Rect from types.hpp
    - Extend existing ICanvas pattern with full rendering API methods:
      - Text: drawText(), setTextColor(), setTextSize(), setFont()
      - Shapes: drawLine(), drawRect(), fillRect(), drawCircle(), fillCircle()
      - Images: drawBitmap() or similar (keep minimal - one method per need)
    - All methods must be pure virtual (= 0)
    - Add virtual destructor `virtual ~ICanvas() = default;`
    - Keep methods minimal and extensible (don't implement all possible operations, just what's needed for migration)
    - Use ICanvas name to match enjin2's existing naming convention
    - Template the interface on TPixel type like enjin2's existing ICanvas
  </action>
  <verify>
    Compile a simple test file including icanvas.hpp
    Verify all methods are pure virtual (= 0)
    Verify virtual destructor exists
  </verify>
  <done>
    icanvas.hpp created with extended rendering API, all methods pure virtual, virtual destructor present
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IComponent abstraction interface</name>
  <files>enjin2/include/enjin2/abstract/icomponent.hpp</files>
  <action>
    Create enjin2/include/enjin2/abstract/icomponent.hpp:
    - Forward declare Object from enjin2::Object
    - Define IComponent class with these pure virtual methods (matching enjin2::Component):
      - `virtual void awake() = 0;`
      - `virtual void start() = 0;`
      - `virtual void update(uint16_t deltaTime) = 0;`
      - `virtual void lateUpdate(uint16_t deltaTime) = 0;`
      - `virtual void onEnable() = 0;`
      - `virtual void onDisable() = 0;`
    - Add virtual destructor `virtual ~IComponent() = default;`
    - Keep minimal - only methods that exist in enjin2::Component
    - Use namespace enjin2 to match existing codebase
    - Do NOT include enjin2/core/component.hpp - use forward declarations
  </action>
  <verify>
    Compile a simple test file including icomponent.hpp
    Verify all methods are pure virtual (= 0)
    Verify virtual destructor exists
    Verify no enjin1 or enjin2 implementation headers are included
  </verify>
  <done>
    icomponent.hpp created with component lifecycle methods, all pure virtual, virtual destructor present
  </done>
</task>

<task type="auto">
  <name>Task 3: Create IScene abstraction interface</name>
  <files>enjin2/include/enjin2/abstract/iscene.hpp</files>
  <action>
    Create enjin2/include/enjin2/abstract/iscene.hpp:
    - Forward declare ICanvas<PixelType> template
    - Define IScene class with these pure virtual methods (matching enjin2::Scene):
      - `virtual void onCreate() = 0;`
      - `virtual void onActivate() = 0;`
      - `virtual void onDeactivate() = 0;`
      - `virtual void onDestroy() = 0;`
      - `virtual void onUpdate(uint16_t deltaTime) = 0;`
      - `virtual void onRender(ICanvas<PixelType>& canvas) = 0;`
    - Add virtual destructor `virtual ~IScene() = default;`
    - Template IScene on PixelType to match enjin2::Scene::render() signature
    - Keep minimal - only methods that exist in enjin2::Scene
    - Use namespace enjin2 to match existing codebase
    - Do NOT include enjin2/core/scene.hpp - use forward declarations
  </action>
  <verify>
    Compile a simple test file including iscene.hpp
    Verify all methods are pure virtual (= 0)
    Verify virtual destructor exists
    Verify template parameter works with different pixel types
  </verify>
  <done>
    iscene.hpp created with scene lifecycle methods, all pure virtual, virtual destructor present, templated on pixel type
  </done>
</task>

</tasks>

<verification>
Compile test files including each abstraction interface header:
1. All abstraction headers compile independently without including implementation headers
2. All methods are pure virtual (= 0)
3. All interfaces have virtual destructors
4. ICanvas extends existing pattern with rendering methods
5. IComponent matches enjin2::Component lifecycle
6. IScene matches enjin2::Scene lifecycle
</verification>

<success_criteria>
Three abstraction interface headers created in enjin2/include/enjin2/abstract/, all with virtual destructors and pure virtual methods matching enjin2's existing API.
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-support/03-02-SUMMARY.md`
</output>
