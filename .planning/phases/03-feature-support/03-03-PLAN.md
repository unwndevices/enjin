---
phase: 03-feature-support
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: ["enjin2/include/enjin2/seams/component_seam.hpp", "enjin2/include/enjin2/seams/scene_seam.hpp"]
autonomous: true

must_haves:
  truths:
    - "ComponentSeam uses compile-time selection via #ifdef USE_ENJIN1_BACKEND"
    - "SceneSeam uses compile-time selection via #ifdef USE_ENJIN1_BACKEND"
    - "Seams implement IComponent and IScene interfaces respectively"
    - "Runtime switching methods (switchToNew, switchToEnjin2) are kept but marked as deprecated"
    - "Builds with USE_ENJIN1=0 compile only enjin2 code paths"
    - "Builds with USE_ENJIN1=1 will compile enjin1 code paths (when enjin1 is integrated)"
  artifacts:
    - path: "enjin2/include/enjin2/seams/component_seam.hpp"
      provides: "Compile-time component seam routing"
      contains: "#ifdef USE_ENJIN1_BACKEND", "class ComponentSeam : public IComponent"
    - path: "enjin2/include/enjin2/seams/scene_seam.hpp"
      provides: "Compile-time scene seam routing"
      contains: "#ifdef USE_ENJIN1_BACKEND", "class SceneSeam : public IScene"
  key_links:
    - from: "component_seam.hpp"
      to: "enjin2/include/enjin2/abstract/icomponent.hpp"
      via: "Inherits from IComponent interface"
      pattern: "ComponentSeam.*:.*public IComponent"
    - from: "scene_seam.hpp"
      to: "enjin2/include/enjin2/abstract/iscene.hpp"
      via: "Inherits from IScene interface"
      pattern: "SceneSeam.*:.*public IScene"
    - from: "component_seam.hpp"
      to: "enjin2/include/enjin2/core/component.hpp"
      via: "Routes to enjin2::Component when USE_ENJIN1_BACKEND=0"
      pattern: "#else.*newImpl.*-\\>awake\\(\\)"
    - from: "component_seam.hpp"
      to: "CMakeLists.txt"
      via: "Uses USE_ENJIN1_BACKEND macro from CMake"
      pattern: "#ifdef USE_ENJIN1_BACKEND"
---

<objective>
Update ComponentSeam and SceneSeam to use compile-time backend selection via USE_ENJIN1_BACKEND macro while implementing IComponent and IScene interfaces.

Purpose: Enable isolated testing of enjin1 and enjin2 builds by routing seam calls through compile-time conditional compilation, while maintaining backward compatibility with existing runtime switching methods (marked deprecated).
Output: Updated seam headers with compile-time routing and interface implementation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-support/03-CONTEXT.md
@.planning/phases/03-feature-support/03-RESEARCH.md
@.planning/phases/02-core-migration/02-02-SUMMARY.md
@.planning/phases/03-feature-support/03-01-PLAN.md
@.planning/phases/03-feature-support/03-02-PLAN.md
@enjin2/include/enjin2/seams/component_seam.hpp
@enjin2/include/enjin2/seams/scene_seam.hpp
@enjin2/include/enjin2/abstract/icomponent.hpp
@enjin2/include/enjin2/abstract/iscene.hpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ComponentSeam for compile-time routing and IComponent interface</name>
  <files>enjin2/include/enjin2/seams/component_seam.hpp</files>
  <action>
    Modify enjin2/include/enjin2/seams/component_seam.hpp:
    - Include "abstract/icomponent.hpp" and make ComponentSeam inherit from IComponent
    - Add `#ifdef USE_ENJIN1_BACKEND` guards around existing routing logic:
      - When USE_ENJIN1_BACKEND=0 (enjin2): Keep existing newImpl routing
      - When USE_ENJIN1_BACKEND=1 (enjin1): Stubbed legacy routing with `#error "enjin1 backend not yet integrated"` or TODO comments
    - Keep existing Implementation enum (LEGACY, NEW) and switchToNew() method for backward compatibility but add [[deprecated]] attribute
    - Keep existing void* legacyImpl storage for future enjin1 integration
    - Ensure all IComponent pure virtual methods are implemented in ComponentSeam
    - Do NOT include enjin1 headers - use forward declarations and opaque void*
    - Use enjin2 namespace for consistency
  </action>
  <verify>
    Compile with -DUSE_ENJIN1=OFF: ComponentSeam compiles and routes to enjin2 implementation
    Attempt compile with -DUSE_ENJIN1=ON: Should fail with "enjin1 backend not yet integrated" error (enjin1 not integrated yet)
  </verify>
  <done>
    ComponentSeam inherits IComponent, uses compile-time #ifdef USE_ENJIN1_BACKEND routing, deprecated runtime switching methods kept
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SceneSeam for compile-time routing and IScene interface</name>
  <files>enjin2/include/enjin2/seams/scene_seam.hpp</files>
  <action>
    Modify enjin2/include/enjin2/seams/scene_seam.hpp:
    - Include "abstract/iscene.hpp" and make SceneSeam inherit from IScene
    - Add `#ifdef USE_ENJIN1_BACKEND` guards around existing routing logic:
      - When USE_ENJIN1_BACKEND=0 (enjin2): Keep existing enjin2Backend routing
      - When USE_ENJIN1_BACKEND=1 (enjin1): Stubbed legacy routing with `#error "enjin1 backend not yet integrated"` or TODO comments
    - Keep existing Backend enum (ENJIN1, ENJIN2) and switchToEnjin2() method for backward compatibility but add [[deprecated]] attribute
    - Keep existing void* enjin1Impl storage for future enjin1 integration
    - Ensure all IScene pure virtual methods are implemented in SceneSeam
    - Do NOT include enjin1 headers - use forward declarations and opaque void*
    - Use enjin2 namespace for consistency
    - Template SceneSeam on PixelType to match IScene interface
  </action>
  <verify>
    Compile with -DUSE_ENJIN1=OFF: SceneSeam compiles and routes to enjin2 implementation
    Attempt compile with -DUSE_ENJIN1=ON: Should fail with "enjin1 backend not yet integrated" error (enjin1 not integrated yet)
  </verify>
  <done>
    SceneSeam inherits IScene, uses compile-time #ifdef USE_ENJIN1_BACKEND routing, deprecated runtime switching methods kept
  </done>
</task>

</tasks>

<verification>
Build enjin2 with both backend configurations:
1. cmake . -DUSE_ENJIN1=OFF: Seams compile and route to enjin2 only, no enjin1 code paths active
2. cmake . -DUSE_ENJIN1=ON: Seams fail with clear error about enjin1 not being integrated yet
3. Both seams implement their respective abstraction interfaces (IComponent, IScene)
4. Deprecated runtime switching methods are present with [[deprecated]] attribute
5. No enjin1 headers are included in seam headers
</verification>

<success_criteria>
ComponentSeam and SceneSeam updated with compile-time USE_ENJIN1_BACKEND routing, implement IComponent and IScene interfaces, deprecated runtime methods retained for compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-support/03-03-SUMMARY.md`
</output>
