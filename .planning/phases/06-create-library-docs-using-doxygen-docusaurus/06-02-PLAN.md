---
phase: 06-create-library-docs-using-doxygen-docusaurus
plan: 02
type: execute
wave: 2
depends_on:
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 01
files_modified:
  - docs/package.json
  - docs/docusaurus.config.js
  - docs/sidebars.js
  - docs/src/
  - docs/static/
autonomous: true
user_setup:
  - Node.js 18+ installed
  - npm available

must_haves:
  truths:
    - "Docusaurus site initialized with classic template"
    - "docusaurus.config.js configured for enjin2 branding and navigation"
    - "Sidebars created for docs structure"
    - "npm install succeeds and npm run build completes"
  artifacts:
    - path: "docs/package.json"
      provides: "Docusaurus project configuration and dependencies"
      contains: "docusaurus >= 3.9"
    - path: "docs/docusaurus.config.js"
      provides: "Site configuration (title, URL, plugins, theme)"
      contains: "title: 'enjin2', url: 'https://unwndevices.github.io/enjin'"
    - path: "docs/sidebars.js"
      provides: "Sidebar navigation structure"
      contains: "docs: [{type: 'category', label: 'Guides', items: [...]}, ...]"
    - path: "docs/src/"
      provides: "Documentation source files"
      contains: "intro.md, getting-started.md"
    - path: "docs/static/"
      provides: "Static assets directory"
      contains: "img/ directory for images"
  key_links:
    - from: "docs/src/**/*.md"
      to: "docs/build/"
      via: "npm run build"
      pattern: "Docusaurus static site generation"
    - from: "docs/xml/index.xml"
      to: "docs/build/"
      via: "doxygen-docusaurus-plugin (future plan)"
      pattern: "Doxygen XML integration (planned for 06-04)"
---

<objective>
Initialize Docusaurus site with enjin2 branding, navigation structure, and basic configuration for documentation presentation.

Purpose: Create the foundation for a modern, searchable documentation website using Docusaurus 3.9+ with proper site configuration and navigation setup.

Output: Working Docusaurus site with enjin2 branding configured, navigation structure defined, and buildable project.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-CONTEXT.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md

# Relevant prior work
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-01-PLAN.md  # Doxygen XML generation setup
</context>

<tasks>

<task type="auto">
  <name>Initialize and configure Docusaurus</name>
  <files>docs/package.json, docs/docusaurus.config.js, docs/sidebars.js</files>
  <action>
    1. Create docs directory: mkdir -p docs

    2. Initialize Docusaurus project:
       cd docs && npx create-docusaurus@latest . classic --skip-install

       This creates:
       - package.json
       - docusaurus.config.js
       - sidebars.js
       - src/ directory structure
       - static/ directory for assets

       Note: Using --skip-install to control npm install step

    3. Remove default blog feature (not needed):
       - Delete docs/blog/ directory if created

    4. Replace docusaurus.config.js with enjin2 configuration:

    ```javascript
    const config = {
      title: 'enjin2 Documentation',
      tagline: 'Lightweight C++ game engine library',
      favicon: 'img/favicon.ico',

      url: 'https://unwndevices.github.io',
      baseUrl: '/enjin/',

      organizationName: 'unwndevices',
      projectName: 'enjin',

      onBrokenLinks: 'throw',
      onBrokenMarkdownLinks: 'warn',

      i18n: {
        defaultLocale: 'en',
        locales: ['en'],
      },

      presets: [
        [
          'classic',
          {
            docs: {
              path: 'src',
              sidebarPath: 'sidebars.js',
              editUrl: 'https://github.com/unwndevices/enjin/edit/main/docs/',
            },
            blog: false,
            theme: {
              customCss: require.resolve('./src/css/custom.css'),
            },
          },
        ],
      ],

      plugins: [],

      themeConfig: {
        image: 'img/docusaurus-social-card.jpg',

        navbar: {
          title: 'enjin2',
          logo: {
            alt: 'enjin2 Logo',
            src: 'img/logo.svg',
          },
          items: [
            {
              type: 'docSidebar',
              sidebarId: 'guidesSidebar',
              position: 'left',
              label: 'Guides',
            },
            {
              type: 'docSidebar',
              sidebarId: 'apiSidebar',
              position: 'left',
              label: 'API Reference',
            },
            {
              href: 'https://github.com/unwndevices/enjin',
              label: 'GitHub',
              position: 'right',
            },
          ],
        },

        footer: {
          style: 'dark',
          links: [
            {
              title: 'Documentation',
              items: [
                { label: 'Getting Started', to: '/docs/getting-started' },
                { label: 'API Reference', to: '/api' },
              ],
            },
            {
              title: 'Community',
              items: [
                { label: 'GitHub', href: 'https://github.com/unwndevices/enjin' },
              ],
            },
            {
              title: 'More',
              items: [
                { label: 'GitHub Issues', href: 'https://github.com/unwndevices/enjin/issues' },
              ],
            },
          ],
          copyright: `Copyright © ${new Date().getFullYear()} UNWN Devices. Built with Docusaurus.`,
        },

        prism: {
          additionalLanguages: ['cpp', 'cmake', 'bash'],
        },
      },
    };

    module.exports = config;
    ```

    5. Replace sidebars.js with enjin2 navigation structure:

    ```javascript
    module.exports = {
      guidesSidebar: [
        'intro',
        'getting-started',
        {
          type: 'category',
          label: 'Core Concepts',
          items: ['architecture', 'components'],
        },
        {
          type: 'category',
          label: 'Graphics',
          items: ['canvas', 'sprites', 'text-rendering'],
        },
        {
          type: 'category',
          label: 'Scenes',
          items: ['scene-management', 'scene-transitions'],
        },
      ],

      apiSidebar: [
        {
          type: 'category',
          label: 'Core',
          items: [
            'api/core/Object',
            'api/core/Component',
            'api/core/Scene',
            'api/core/SceneStateMachine',
          ],
        },
        {
          type: 'category',
          label: 'Graphics',
          items: [
            'api/graphics/Canvas',
            'api/graphics/Sprite',
            'api/graphics/TextRenderer',
          ],
        },
        {
          type: 'category',
          label: 'Utils',
          items: [
            'api/utils/DrawingHelpers',
            'api/utils/Noise',
            'api/utils/Polar',
          ],
        },
      ],
    };
    ```

    Reference: 06-RESEARCH.md "Installation" and "Docusaurus Configuration for API Docs"
  </action>
  <verify>test -f docs/package.json && test -f docs/docusaurus.config.js && test -f docs/sidebars.js && grep -q "title: 'enjin2 Documentation'" docs/docusaurus.config.js && grep -q "baseUrl: '/enjin/'" docs/docusaurus.config.js && grep -q "guidesSidebar" docs/sidebars.js && grep -q "apiSidebar" docs/sidebars.js</verify>
  <done>Docusaurus initialized with enjin2 branding, GitHub Pages URL, and navigation structure</done>
</task>

<task type="auto">
  <name>Create content, build site, and configure .gitignore</name>
  <files>docs/src/*.md, docs/static/, docs/src/css/custom.css, docs/node_modules/, docs/build/, .gitignore</files>
  <action>
    1. Create placeholder documentation pages (will be filled in plan 06-03):

    - docs/src/intro.md: enjin2 overview with key features
    - docs/src/getting-started.md: 3-step setup (Clone → Configure → Build)
    - docs/src/architecture.md: placeholder
    - docs/src/components.md: placeholder
    - docs/src/canvas.md: placeholder
    - docs/src/sprites.md: placeholder
    - docs/src/text-rendering.md: placeholder
    - docs/src/scene-management.md: placeholder
    - docs/src/scene-transitions.md: placeholder

    Each placeholder should have minimal frontmatter and "TODO" note.

    2. Create static assets directories:
       mkdir -p docs/static/img docs/static/fonts

    3. Create docs/src/css/custom.css for custom styling:

    ```css
    :root {
      --ifm-font-family-base: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
      --ifm-color-primary: #2e8555;
      --ifm-color-primary-dark: #29784c;
      --ifm-color-primary-darker: #277148;
      --ifm-color-primary-darkest: #205d3b;
      --ifm-color-primary-light: #33925d;
      --ifm-color-primary-lighter: #359962;
      --ifm-color-primary-lightest: #3cad6e;
    }

    pre code {
      font-size: 0.9em;
    }

    .api-member {
      margin-bottom: 1.5rem;
    }
    ```

    4. Add placeholder logo: Create docs/static/img/logo.svg (text-based SVG placeholder)

    5. Install npm dependencies:
       cd docs && npm install

    6. Build site to verify configuration:
       cd docs && npm run build

    7. Verify build output:
       - Check docs/build/index.html exists
       - Verify no build errors or critical warnings

    8. Update .gitignore to exclude build artifacts but keep source files:

    ```
    # Docusaurus build artifacts
    docs/build/
    docs/.docusaurus/
    docs/node_modules/

    # Keep documentation source files and configuration
    !docs/src/
    !docs/static/
    !docs/docusaurus.config.js
    !docs/sidebars.js
    !docs/package.json
    !docs/package-lock.json
    ```

    Reference: 06-CONTEXT.md and 06-RESEARCH.md
  </action>
  <verify>test -d docs/static/img && test -f docs/src/css/custom.css && test -f docs/static/img/logo.svg && ls docs/src/*.md | wc -l | grep -q "9" && test -d docs/node_modules && test -f docs/build/index.html && grep -q "^docs/build/" .gitignore && grep -q "^docs/node_modules/" .gitignore</verify>
  <done>9 placeholder pages created, static assets directory created, custom CSS configured, placeholder logo added, npm install succeeded, site builds successfully, .gitignore configured</done>
</task>

</tasks>

<verification>
After completing all tasks, verify the phase success criteria:

1. Docusaurus site is initialized:
   - docs/package.json exists with docusaurus dependency
   - docs/docusaurus.config.js configured for enjin2
   - docs/sidebars.js has Guides and API sidebars
   - docs/src/ has markdown files
   - docs/static/ has assets

2. Configuration is correct:
   - Title: "enjin2 Documentation"
   - URL: https://unwndevices.github.io/enjin
   - Navbar has Guides, API Reference, GitHub
   - Blog is disabled

3. Build works:
   - npm install succeeds
   - npm run build completes without errors
   - docs/build/index.html exists
   - No critical warnings in build output

4. Navigation structure:
   - Guides sidebar has intro, getting-started, categories
   - API sidebar has module structure (placeholder)
   - All placeholder pages exist
</verification>

<success_criteria>
1. docs/package.json exists and is valid JSON
2. docs/docusaurus.config.js has enjin2 branding and GitHub Pages URL (baseUrl='/enjin/')
3. docs/sidebars.js has guidesSidebar and apiSidebar configured
4. docs/src/ has 9 markdown placeholder files
5. docs/static/img/logo.svg exists
6. docs/src/css/custom.css exists with enjin2 branding colors
7. npm install succeeds
8. npm run build completes without errors
9. docs/build/index.html exists
10. .gitignore excludes docs/build/, docs/.docusaurus/, docs/node_modules/
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-02-SUMMARY.md`
</output>
