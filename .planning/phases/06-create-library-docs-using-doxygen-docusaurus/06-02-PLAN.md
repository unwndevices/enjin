---
phase: 06-create-library-docs-using-doxygen-docusaurus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/package.json
  - docs/docusaurus.config.js
  - docs/sidebars.js
  - docs/docs/
autonomous: true

must_haves:
  truths:
    - "Docusaurus project initialized in docs/ directory"
    - "docusaurus.config.js configured for enjin2"
    - "Site can build successfully with npm run build"
  artifacts:
    - path: "docs/package.json"
      provides: "Docusaurus dependencies and scripts"
      contains: "\"name\":\\s*\"enjin2-docs\""
    - path: "docs/docusaurus.config.js"
      provides: "Docusaurus site configuration"
      contains: "title.*enjin2"
    - path: "docs/build/"
      provides: "Built Docusaurus site"
      exists: true
    - path: "docs/sidebars.js"
      provides: "Documentation sidebar structure"
      exists: true
  key_links:
    - from: "docs/docusaurus.config.js"
      to: "docs/src/"
      via: "docs plugin configuration"
      pattern: "docs.*path.*src"
    - from: "docs/sidebars.js"
      to: "docs/src/"
      via: "sidebarPath configuration"
      pattern: "sidebarPath.*sidebars"
---

<objective>
Initialize Docusaurus static site generator project for publishing enjin2 documentation with modern web interface and search capabilities.

Purpose: Set up a modern, React-based documentation site that provides excellent user experience, navigation, and search capabilities for enjin2 API documentation and user guides.

Output: Docusaurus project structure in docs/ with configuration files, navigation structure, and build system ready for content.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md

# Research recommendations
# Use Docusaurus 3.9+ with classic theme
# Configure plugins for API docs and search
# Set up navigation structure for guides and API reference
</context>

<tasks>

<task type="auto">
  <name>Initialize Docusaurus project</name>
  <files>docs/</files>
  <action>
    1. Create docs/ directory if it doesn't exist (already created by plan 06-01)
    2. Initialize Docusaurus project using create-docusaurus:
       cd docs && npx create-docusaurus@latest . classic --yes
       Note: Use '.' to initialize in current directory (docs/)
    3. Remove generated example content (keep structure):
       - Remove docs/blog/ (not needed)
       - Keep docs/src/ and docs/static/
    4. Verify package.json was created

    Reference: .planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md (Installation section)

    Note: If npx prompts for confirmation, use --yes flag to auto-confirm.
  </action>
  <verify>test -f docs/package.json && test -f docs/docusaurus.config.js</verify>
  <done>Docusaurus project initialized in docs/ directory</done>
</task>

<task type="auto">
  <name>Configure docusaurus.config.js for enjin2</name>
  <files>docs/docusaurus.config.js</files>
  <action>
    1. Read existing docusaurus.config.js
    2. Modify configuration with enjin2-specific settings:
       const config = {
         title: 'enjin2 Documentation',
         tagline: 'Lightweight C++ game engine library',
         favicon: 'img/favicon.ico',

         url: 'https://unwndevices.github.io',
         baseUrl: '/enjin/',

         organizationName: 'unwndevices',
         projectName: 'enjin',

         onBrokenLinks: 'throw',
         onBrokenMarkdownLinks: 'warn',

         presets: [
           [
             'classic',
              {
                docs: {
                  path: 'docs',
                  sidebarPath: 'sidebars.js',
                  editUrl: null,  // Auto-generated, not editable
                },
               blog: false,  // No blog needed
               theme: {
                 customCss: './src/css/custom.css',
               },
             },
           ],
         ],

         themeConfig: {
           navbar: {
             title: 'enjin2',
             items: [
               {to: '/docs/intro', label: 'Introduction', position: 'left'},
               {to: '/docs/guides/getting-started', label: 'Guides', position: 'left'},
               {to: '/api', label: 'API Reference', position: 'left'},
               {href: 'https://github.com/unwndevices/enjin', label: 'GitHub', position: 'right'},
             ],
           },
           footer: {
             style: 'dark',
             copyright: `Copyright © ${new Date().getFullYear()} unwndevices. Built with Docusaurus.`,
           },
           prism: {
             additionalLanguages: ['cpp', 'cmake', 'lua'],
           },
         },
       };

    Reference: .planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md (Docusaurus Configuration section)

    Note: baseUrl should match GitHub Pages repository name (/enjin/).
  </action>
  <verify>grep "enjin2 Documentation" docs/docusaurus.config.js</verify>
  <done>docusaurus.config.js configured with enjin2 branding and navigation</done>
</task>

<task type="auto">
  <name>Set up sidebars.js for navigation structure</name>
  <files>docs/sidebars.js</files>
  <action>
    1. Read existing sidebars.js
    2. Configure sidebar structure:
       module.exports = {
         docs: [
           'intro',
           {
             type: 'category',
             label: 'Guides',
             items: [
               'guides/getting-started',
               'guides/architecture',
               'guides/components',
               'guides/scenes',
               'guides/graphics',
               'guides/lua-scripting',
             ],
           },
           {
             type: 'category',
             label: 'API Reference',
             items: [
               'api/core',
               'api/graphics',
               'api/components',
               'api/ui',
               'api/scripting',
               'api/utils',
             ],
           },
         ],
       };

    Reference: .planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md (Hybrid Documentation pattern)

    Note: This creates a clear separation between hand-written guides and API reference sections.
  </action>
  <verify>test -f docs/sidebars.js && grep -q "Guides" docs/sidebars.js</verify>
  <done>sidebars.js configured with navigation for guides and API reference</done>
</task>

<task type="auto">
  <name>Verify Docusaurus site builds successfully</name>
  <files>docs/build/</files>
  <action>
    1. Install Docusaurus dependencies:
       cd docs && npm install
    2. Build the site:
       cd docs && npm run build
    3. Verify build output exists:
       ls -la docs/build/
    4. Check that index.html was generated:
       test -f docs/build/index.html && echo "index.html exists"

    Reference: .planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md (Pitfall 5: Deployment to GitHub Pages Fails)

    Note: Build should succeed even without all content files (placeholder content is fine).
  </action>
  <verify>test -d docs/build && test -f docs/build/index.html</verify>
  <done>Docusaurus site builds successfully in docs/build/ directory</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. docs/package.json exists with Docusaurus dependencies
2. docs/docusaurus.config.js configured for enjin2 with proper URL/baseUrl
3. docs/sidebars.js defines navigation structure
4. Running `cd docs && npm run build` completes successfully
5. docs/build/ directory contains generated static site files
</verification>

<success_criteria>
Docusaurus project is initialized and configured:
- Project initialized in docs/ with package.json and docusaurus.config.js
- Configuration includes enjin2 branding, navigation, and GitHub Pages settings
- Navigation structure defined in sidebars.js
- Site builds successfully with npm run build
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-02-SUMMARY.md`
</output>
