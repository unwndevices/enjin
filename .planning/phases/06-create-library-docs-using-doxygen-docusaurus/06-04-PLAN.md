---
phase: 06-create-library-docs-using-doxygen-docusaurus
plan: 04
type: execute
wave: 3
depends_on:
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 01
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 02
files_modified:
  - docs/src/api/
  - docs/docusaurus.config.js
  - docs/sidebars.js
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Doxygen XML consumed and converted to Docusaurus pages"
    - "API reference organized by module (core, graphics, ui, utils, components, etc.)"
    - "All public enjin2 APIs documented with minimal but complete descriptions"
    - "API sidebar auto-generated from Doxygen XML structure"
    - "Cross-links from guides to API reference pages"
    - "No placeholder TODOs - all API pages have real content"
  artifacts:
    - path: "docs/src/api/"
      provides: "Auto-generated API reference pages from Doxygen XML"
      contains: "modules: core, graphics, ui, utils, components, animation, effects, scripting, abstract"
    - path: "docs/docusaurus.config.js"
      provides: "Docusaurus config with API docs plugin"
      contains: "@docusaurus/plugin-content-docs for API reference"
    - path: "docs/sidebars.js"
      provides: "Auto-generated API sidebar structure"
      contains: "apiSidebar with module categories"
  key_links:
    - from: "docs/xml/index.xml"
      to: "docs/src/api/**/*.md"
      via: "XML parsing and markdown generation"
      pattern: "Doxygen XML → Docusaurus pages conversion"
    - from: "docs/src/components.md"
      to: "docs/src/api/core/Component.md"
      via: "Cross-link in See Also section"
      pattern: "Guide to reference cross-links"
    - from: "docs/src/canvas.md"
      to: "docs/src/api/graphics/Canvas.md"
      via: "Cross-link in See Also section"
      pattern: "Guide to reference cross-links"
---

<objective>
Create API reference pages by consuming Doxygen XML and organizing enjin2 APIs by module, with cross-links from guides to reference pages.

Purpose: Generate comprehensive API documentation from existing code comments (3031 lines across 50+ headers) organized by functional modules for easy navigation.

Output: Complete API reference with all public enjin2 APIs documented, organized by module, with sidebar navigation and guide cross-links.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-CONTEXT.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md

# Relevant prior work
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-01-PLAN.md  # Doxygen XML generation
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-02-PLAN.md  # Docusaurus initialization
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-03-PLAN.md  # Core guide content
</context>

<tasks>

<task type="auto">
  <name>Configure API docs infrastructure</name>
  <files>docs/docusaurus.config.js, docs/api-sidebar.js, docs/src/api/</files>
  <action>
    1. Update docusaurus.config.js to add second docs instance for API reference:

    ```javascript
    plugins: [
      [
        '@docusaurus/plugin-content-docs',
        {
          id: 'api',
          path: 'api',
          routeBasePath: 'api',
          sidebarPath: 'api-sidebar.js',
          editUrl: null,
          showLastUpdateAuthor: false,
          showLastUpdateTime: false,
        },
      ],
    ],
    ```

    2. Create API directory structure by module:

    ```bash
    mkdir -p docs/src/api/core
    mkdir -p docs/src/api/graphics
    mkdir -p docs/src/api/ui
    mkdir -p docs/src/api/utils
    mkdir -p docs/src/api/components
    mkdir -p docs/src/api/animation
    mkdir -p docs/src/api/effects
    mkdir -p docs/src/api/scripting
    mkdir -p docs/src/api/abstract
    ```

    3. Create docs/api-sidebar.js with module-based structure derived from enjin2 headers, including all classes from include/enjin2/ (Core, Graphics, UI, Utils, Components, Animation, Effects, Scripting, Abstract)

    Reference: 06-RESEARCH.md "Pattern 2: Docusaurus XML Consumption" and "Recommended Project Structure"
  </action>
  <verify>grep -q "id: 'api'" docs/docusaurus.config.js && grep -q "routeBasePath: 'api'" docs/docusaurus.config.js && test -f docs/api-sidebar.js && test -d docs/src/api/core && test -d docs/src/api/graphics</verify>
  <done>API docs plugin configured, directory structure created, sidebar generated with 9 module categories</done>
</task>

<task type="auto">
  <name>Implement and run API generation</name>
  <files>scripts/generate-api-docs.js, docs/src/api/ (generated)</files>
  <action>
    1. Create scripts/generate-api-docs.js to parse Doxygen XML and generate markdown pages using xml2js library

    2. Add xml2js dependency to docs/package.json:
       ```json
       {
         "devDependencies": {
           "xml2js": "^0.4.23"
         }
       }
       ```

    3. Install xml2js: cd docs && npm install --save-dev xml2js

    4. Ensure Doxygen XML is generated: cmake --build build --target docs

    5. Run API generation script: node scripts/generate-api-docs.js

    6. Verify generated pages:
       - All 9 module directories have markdown files
       - Pages have proper frontmatter (title, sidebar_label)
       - Pages contain sections (Methods, Members, Types)

    Reference: 06-RESEARCH.md "Don't Hand-Roll" - use xml2js, "Pitfall 2: Documentation Inconsistent with Code"
  </action>
  <verify>test -f scripts/generate-api-docs.js && grep -q "xml2js" docs/package.json && ls docs/src/api/core/*.md | wc -l | grep -q "[0-9]"</verify>
  <done>generate-api-docs.js script created, xml2js installed, API markdown pages generated from Doxygen XML</done>
</task>

<task type="auto">
  <name>Integrate API reference and update CMake docs target</name>
  <files>docs/src/components.md, docs/src/canvas.md, docs/src/scene-management.md, docs/src/architecture.md, docs/build/ (generated), CMakeLists.txt</files>
  <action>
    1. Update guide pages to include cross-links to API reference pages in "See Also" sections:
       - docs/src/components.md → [API: Component](/api/core/Component), [API: SceneObject](/api/core/SceneObject)
       - docs/src/canvas.md → [API: Canvas](/api/graphics/Canvas), [API: CanvasExtended](/api/graphics/CanvasExtended)
       - docs/src/scene-management.md → [API: Scene](/api/core/Scene), [API: SceneStateMachine](/api/core/SceneStateMachine), [API: SceneTransition](/api/core/SceneTransition)
       - docs/src/architecture.md → [API: SceneStateMachine](/api/core/SceneStateMachine), [API: Signal](/api/core/Signal)
       - docs/src/sprites.md → [API: Sprite](/api/graphics/Sprite)
       - docs/src/text-rendering.md → [API: TextRenderer](/api/graphics/TextRenderer)
       - docs/src/scene-transitions.md → [API: SceneTransition](/api/core/SceneTransition)

    2. Build site: cd docs && npm run build

    3. Verify build output:
       - /api route is generated
       - API pages render correctly
       - API sidebar navigation works
       - Cross-links from guides to API work

    4. Review content quality:
       - Brief descriptions present for all classes
       - Method signatures formatted correctly
       - No "TODO" or placeholder content

    5. Update CMakeLists.txt to include API generation in docs target:

    ```cmake
    add_custom_target(docs
        COMMAND ${DOXYGEN_EXECUTABLE} ${DOXYGEN_IN}
        COMMAND node ${CMAKE_CURRENT_SOURCE_DIR}/scripts/generate-api-docs.js
        WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
        COMMENT "Generating API documentation with Doxygen"
        BYPRODUCTS ${DOXYGEN_OUT}/index.xml
    )
    ```

    Now running `cmake --build . --target docs` generates Doxygen XML and API markdown pages.

    Reference: 06-RESEARCH.md "Pitfall 4: Large XML Files Cause Slow Builds" and "Pattern 1: Doxygen XML Generation"
  </action>
  <verify>grep -q "\[API:" docs/src/components.md && grep -q "\[API:" docs/src/canvas.md && grep -q "\[API:" docs/src/scene-management.md && npm run build in docs/ succeeds && grep -q "node.*generate-api-docs.js" CMakeLists.txt</verify>
  <done>Guide cross-links updated, site builds successfully, API reference accessible at /api route, docs target updated to run API generation script after doxygen</done>
</task>

</tasks>

<verification>
After completing all tasks, verify the phase success criteria:

1. Docusaurus configuration:
   - API docs plugin configured with /api route
   - Separate sidebar for API reference

2. API structure:
   - All 9 modules have markdown pages
   - Pages organized by module (core, graphics, ui, utils, etc.)
   - Sidebar reflects module structure

3. Content quality:
   - All public APIs have brief descriptions
   - Method signatures documented
   - No TODO placeholders

4. Cross-links:
   - Guides have API reference links in See Also sections
   - Links work and navigate to correct pages

5. Build:
   - Site builds without errors
   - /api route accessible
   - API navigation works

6. Automation:
   - CMake docs target generates both XML and API pages
   - Single command generates all documentation
</verification>

<success_criteria>
1. docs/docusaurus.config.js has API docs plugin with id: 'api' and routeBasePath: 'api'
2. docs/src/api/ has subdirectories for all 9 modules (core, graphics, ui, utils, components, animation, effects, scripting, abstract)
3. docs/api-sidebar.js has module-based sidebar structure with all enjin2 classes
4. scripts/generate-api-docs.js parses Doxygen XML and generates markdown
5. docs/package.json has xml2js dependency
6. API markdown pages generated from Doxygen XML with proper frontmatter and sections
7. All guide pages have API cross-links in See Also sections
8. npm run build succeeds with no errors
9. /api route is accessible and renders API pages correctly
10. CMakeLists.txt docs target runs both doxygen and generate-api-docs.js
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-04-SUMMARY.md`
</output>
