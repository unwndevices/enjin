---
phase: 06-create-library-docs-using-doxygen-docusaurus
plan: 05
type: execute
wave: 5
depends_on:
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 01
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 02
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 03
  - phase: 06-create-library-docs-using-doxygen-docusaurus
    plan: 04
files_modified:
  - .github/workflows/docs.yml
  - docs/docusaurus.config.js
autonomous: true
user_setup:
  - GitHub repository configured with Actions enabled
  - GitHub Pages enabled for repository

must_haves:
  truths:
      - "GitHub Actions workflow builds and deploys docs on main branch push"
      - "Workflow installs Doxygen, generates XML, runs API generation, builds Docusaurus"
      - "Deployment uses actions/deploy-pages@v4 for GitHub Pages"
      - "GitHub Pages configured for /enjin subdirectory or gh-pages branch"
      - "Deployment succeeds and docs are accessible at GitHub Pages URL"
  artifacts:
      - path: ".github/workflows/docs.yml"
        provides: "CI/CD workflow for automated documentation deployment"
        contains: "doxygen install, xml generation, docusaurus build, gh-pages deployment"
      - path: "docs/docusaurus.config.js"
        provides: "Docusaurus config with GitHub Pages URL"
        contains: "url: 'https://unwndevices.github.io', baseUrl: '/enjin/'"
  key_links:
      - from: ".github/workflows/docs.yml"
        to: "GitHub Pages"
        via: "actions/deploy-pages@v4 action"
        pattern: "Automated deployment on main branch push"
      - from: "docs/build/"
        to: "https://unwndevices.github.io/enjin/"
        via: "GitHub Pages hosting"
        pattern: "Static site deployment"
---

<objective>
Set up CI/CD pipeline for automated documentation deployment to GitHub Pages on main branch pushes.

Purpose: Enable automated documentation generation and deployment, ensuring docs stay synchronized with code changes without manual intervention.

Output: GitHub Actions workflow that builds docs and deploys to GitHub Pages on every push to main branch.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-CONTEXT.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-RESEARCH.md

# Relevant prior work
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-01-PLAN.md  # Doxygen XML generation
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-02-PLAN.md  # Docusaurus initialization
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-03-PLAN.md  # Core guide content
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-04-PLAN.md  # API reference generation
</context>

<tasks>

<task type="auto">
  <name>Create and configure GitHub Actions workflow</name>
  <files>.github/workflows/docs.yml, docs/docusaurus.config.js</files>
  <action>
    1. Create .github/workflows/docs.yml with complete CI/CD pipeline:

    ```yaml
    name: Deploy Documentation

    on:
      push:
        branches: [main]
        paths:
          - 'docs/**'
          - 'include/**'
          - '.github/workflows/docs.yml'
      pull_request:
        paths:
          - 'docs/**'
          - 'include/**'
          - '.github/workflows/docs.yml'

    jobs:
      deploy:
        name: Deploy to GitHub Pages
        runs-on: ubuntu-latest
        permissions:
          contents: read
          pages: write
          id-token: write

        steps:
          - name: Checkout repository
            uses: actions/checkout@v4

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: 18
              cache: 'npm'
              cache-dependency-path: docs/package-lock.json

          - name: Install dependencies
            run: |
              cd docs
              npm ci

          - name: Install Doxygen and Graphviz
            run: |
              sudo apt-get update
              sudo apt-get install -y doxygen graphviz

          - name: Generate Doxygen XML
            run: |
              mkdir -p build
              cd build
              cmake .. -DCMAKE_BUILD_TYPE=Release
              cmake --build . --target docs

          - name: Generate API documentation
            run: |
              node scripts/generate-api-docs.js

          - name: Build Docusaurus site
            run: |
              cd docs
              npm run build

          - name: Setup Pages
            uses: actions/configure-pages@v4

          - name: Upload artifact
            uses: actions/upload-pages-artifact@v3
            with:
              path: ./docs/build

          - name: Deploy to GitHub Pages
            if: github.ref == 'refs/heads/main'
            id: deployment
            uses: actions/deploy-pages@v4
    ```

    2. Verify and update docs/docusaurus.config.js for GitHub Pages deployment:

    Check that these settings are correct:
    ```javascript
    url: 'https://unwndevices.github.io',
    baseUrl: '/enjin/',
    organizationName: 'unwndevices',
    projectName: 'enjin',
    ```

    Reference: 06-RESEARCH.md "GitHub Actions Workflow for Docs Deployment"
  </action>
  <verify>test -f .github/workflows/docs.yml && grep -q "deploy-pages@v4" .github/workflows/docs.yml && grep -q "doxygen" .github/workflows/docs.yml && grep -q "url: 'https://unwndevices.github.io'" docs/docusaurus.config.js && grep -q "baseUrl: '/enjin/'" docs/docusaurus.config.js</verify>
  <done>GitHub Actions workflow created and Docusaurus configured for GitHub Pages deployment</done>
</task>

<task type="auto">
  <name>Set up local testing</name>
  <files>scripts/deploy-docs.sh, docs/build/ (generated)</files>
  <action>
    1. Create scripts/deploy-docs.sh for local testing of deployment:

    ```bash
    #!/bin/bash
    set -e
    echo "Building documentation locally..."

    if [ ! -d "docs/node_modules" ]; then
        echo "Installing npm dependencies..."
        cd docs && npm install && cd ..
    fi

    echo "Generating Doxygen XML..."
    mkdir -p build
    cd build
    cmake .. -DCMAKE_BUILD_TYPE=Release 2>/dev/null || cmake ..
    cmake --build . --target docs
    cd ..

    echo "Generating API documentation..."
    node scripts/generate-api-docs.js

    echo "Building Docusaurus site..."
    cd docs
    npm run build
    cd ..

    echo "Serving site at http://localhost:8080"
    echo "Press Ctrl+C to stop"
    cd docs/build
    python3 -m http.server 8080
    ```

    chmod +x scripts/deploy-docs.sh

    2. Run local deployment test:
       ./scripts/deploy-docs.sh

    3. Verify build completes and test local server at http://localhost:8080

    4. Review build output for warnings and note any critical issues

    Reference: 06-RESEARCH.md "Pitfall 5: Deployment to GitHub Pages Fails"
  </action>
  <verify>test -f scripts/deploy-docs.sh && test -x scripts/deploy-docs.sh && test -d docs/build</verify>
  <done>deploy-docs.sh script created and local deployment test succeeds</done>
</task>

<task type="auto">
  <name>Finalize deployment setup</name>
  <files>docs/deployment.md, README.md, .github/workflows/docs.yml, docs/docusaurus.config.js, scripts/deploy-docs.sh</files>
  <action>
    Create deployment documentation, verify GitHub settings, and prepare files for commit:

    1. Create docs/deployment.md with sections:
       - Overview (deployment URL)
       - Deployment Process (steps on main branch push)
       - Deployment Status (check in GitHub Actions)
       - Local Preview (using deploy-docs.sh)
       - Manual Deployment (npm run deploy)
       - Troubleshooting (deployment fails, 404, broken links)

    2. Update README.md to include link to documentation:

    ```markdown
    ## Documentation

    Full documentation is available at: https://unwndevices.github.io/enjin/

    - [Getting Started](https://unwndevices.github.io/enjin/docs/getting-started) - Quick setup guide
    - [API Reference](https://unwndevices.github.io/enjin/api) - Complete API documentation
    - [Architecture](https://unwndevices.github.io/enjin/docs/architecture) - Design overview
    ```

    3. Verify GitHub repository configuration:
       - Check GitHub Actions is enabled at https://github.com/unwndevices/enjin/actions
       - Check GitHub Pages is enabled at https://github.com/unwndevices/enjin/settings/pages
       - If not enabled, provide instructions:
         - Go to repository Settings > Pages
         - Enable GitHub Pages
         - Set source to "GitHub Actions"

    4. Prepare commit message for deployment files:

    ```
    docs: Add automated documentation deployment to GitHub Pages

    - Add GitHub Actions workflow for automated docs deployment
    - Configure Docusaurus for GitHub Pages (unwndevices.github.io/enjin)
    - Add local deployment test script
    - Add deployment guide for reference
    - Update README with documentation link

    Workflow generates Doxygen XML, creates API pages, builds Docusaurus,
    and deploys to GitHub Pages on main branch pushes.
    ```

    Files to commit:
    - .github/workflows/docs.yml
    - docs/docusaurus.config.js (if modified)
    - scripts/deploy-docs.sh
    - docs/deployment.md
    - README.md

    Note: Actual commit and push should be done by user to trigger deployment.

    Reference: 06-RESEARCH.md "Common Pitfalls" - deployment troubleshooting
  </action>
  <verify>test -f docs/deployment.md && grep -q "GitHub Pages" docs/deployment.md && grep -q "unwndevices.github.io/enjin" README.md</verify>
  <done>deployment.md created, README updated, GitHub settings verified, files ready for commit</done>
</task>

</tasks>

<verification>
After completing all tasks, verify the phase success criteria:

1. GitHub Actions workflow:
   - .github/workflows/docs.yml exists
   - Workflow triggers on main branch push
   - Workflow builds docs and deploys to GitHub Pages
   - Uses official GitHub Pages actions

2. Docusaurus configuration:
   - URL: https://unwndevices.github.io
   - Base URL: /enjin/
   - Matches GitHub Pages configuration

3. Local testing:
   - deploy-docs.sh script exists and works
   - Local build succeeds
   - Site serves correctly at localhost

4. Documentation:
   - deployment.md guide exists
   - README.md has link to documentation
   - Troubleshooting section covers common issues

5. GitHub configuration:
   - GitHub Actions enabled
   - GitHub Pages enabled (or instructions provided)
   - Workflow ready to run on push

6. Files ready:
   - All deployment files ready for commit
   - Ready for user to commit and push to trigger deployment
</verification>

<success_criteria>
1. .github/workflows/docs.yml exists with complete build and deployment workflow
2. docs/docusaurus.config.js has correct GitHub Pages URL and baseUrl
3. scripts/deploy-docs.sh exists and is executable
4. docs/deployment.md exists with deployment process, local preview, and troubleshooting info
5. README.md has link to https://unwndevices.github.io/enjin/
6. Local deployment test succeeds (./scripts/deploy-docs.sh)
7. GitHub repository settings verified (Actions enabled, Pages enabled or instructions provided)
8. All deployment files ready for commit (workflow, config, script, docs, README)
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-05-SUMMARY.md`
</output>
