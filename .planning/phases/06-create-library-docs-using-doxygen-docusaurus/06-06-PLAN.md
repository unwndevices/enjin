---
phase: 06-create-library-docs-using-doxygen-docusaurus
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [docs/docusaurus.config.js, docs/sidebars.js, docs/api-sidebar.js]
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "API reference accessible through Docusaurus site navigation"
    - "API reference pages have structured navigation with module-based sidebar"
    - "Navbar shows 'API Reference' link that reveals module structure"
    - "API pages render with left sidebar showing module categories"
  artifacts:
    - path: "docs/api-sidebar.js"
      provides: "Module-based navigation for API reference"
      min_lines: 70
      contains: "abstract, animation, components, core, effects, graphics, scripting, ui, utils"
    - path: "docs/docusaurus.config.js"
      provides: "Enabled API docs plugin with /api route"
      contains: "@docusaurus/plugin-content-docs"
      not_contains: "// TODO: API plugin temporarily disabled"
    - path: "docs/sidebars.js"
      provides: "API sidebar configuration"
      contains: "apiSidebar:"
      not_contains: "// TODO: Re-enable API sidebar"
  key_links:
    - from: "docs/docusaurus.config.js (API plugin)"
      to: "docs/src/api/**/*.md"
      via: "@docusaurus/plugin-content-docs with routeBasePath: 'api'"
      pattern: "routeBasePath: 'api'"
    - from: "docs/api-sidebar.js"
      to: "docs/src/api/**/*.md"
      via: "module-based sidebar structure referencing all 59 API files"
    - from: "docs/docusaurus.config.js (navbar)"
      to: "docs/api-sidebar.js"
      via: "docSidebar with apiSidebar ID"
      pattern: "sidebarId: 'apiSidebar'"
---

<objective>
Enable API reference navigation in Docusaurus site by uncommenting plugin configuration, creating sidebar file, and verifying navigation structure works.

Purpose: Close verification gaps where API pages exist (59 files) but are not discoverable through site navigation due to commented-out configuration
Output: Working API reference navigation with module-based sidebar and navbar link
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap closure context
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-create-library-docs-using-doxygen-docusaurus-VERIFICATION.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-04-SUMMARY.md
@.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-05-SUMMARY.md

# Existing files being modified
@docs/docusaurus.config.js
@docs/sidebars.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api-sidebar.js with complete module navigation</name>
  <files>docs/api-sidebar.js</files>
  <action>Create docs/api-sidebar.js with module-based sidebar structure matching all 59 generated API files:

```javascript
module.exports = {
  apiSidebar: [
    {
      type: 'category',
      label: 'Abstract',
      items: [
        'api/abstract/ICanvas',
        'api/abstract/IComponent',
        'api/abstract/IScene',
      ],
    },
    {
      type: 'category',
      label: 'Animation',
      items: [
        'api/animation/AnimationSystem',
        'api/animation/AnimationTrack',
        'api/animation/EasingFunctions',
      ],
    },
    {
      type: 'category',
      label: 'Components',
      items: [
        'api/components/ButtonDial',
        'api/components/FillUpGauge',
        'api/components/Label',
        'api/components/Slider',
        'api/components/Tickmarks',
      ],
    },
    {
      type: 'category',
      label: 'Core',
      items: [
        'api/core/Component',
        'api/core/ComponentBase',
        'api/core/ComponentQuery',
        'api/core/ComponentStorage',
        'api/core/EntityManager',
        'api/core/HandlePool',
        'api/core/Object',
        'api/core/ObjectCollection',
        'api/core/Scene',
        'api/core/SceneStateMachine',
        'api/core/Signal',
        'api/core/SignalConnection',
        'api/core/StackAllocator',
        'api/core/StaticPool',
      ],
    },
    {
      type: 'category',
      label: 'Effects',
      items: [
        'api/effects/Effects',
        'api/effects/PostFx',
      ],
    },
    {
      type: 'category',
      label: 'Graphics',
      items: [
        'api/graphics/Canvas',
        'api/graphics/Canvas4',
        'api/graphics/Canvas8',
        'api/graphics/CanvasExtended',
        'api/graphics/CanvasGraphicsAdapter',
        'api/graphics/ImageExporter',
        'api/graphics/PackedPixel4',
        'api/graphics/Primitives',
        'api/graphics/RenderSystem',
        'api/graphics/Sprite',
        'api/graphics/TextRenderer',
      ],
    },
    {
      type: 'category',
      label: 'Scripting',
      items: [
        'api/scripting/FileInterface',
        'api/scripting/IScriptGraphics',
        'api/scripting/IScriptInterpreter',
        'api/scripting/LuaBindings',
        'api/scripting/LuaCanvas',
        'api/scripting/LuaEngine',
        'api/scripting/LuaFileSystem',
        'api/scripting/LuaInterpreter',
        'api/scripting/LuaPlatform',
        'api/scripting/LuaScriptSystem',
        'api/scripting/MinimalLuaInterpreter',
        'api/scripting/ScriptFactory',
      ],
    },
    {
      type: 'category',
      label: 'UI',
      items: [
        'api/ui/System',
        'api/ui/SystemBase',
        'api/ui/SystemManager',
      ],
    },
    {
      type: 'category',
      label: 'Utils',
      items: [
        'api/utils/Colors',
        'api/utils/DrawingHelpers',
        'api/utils/InputSystem',
        'api/utils/math',
        'api/utils/Noise',
        'api/utils/Polar',
        'api/utils/Signals',
      ],
    },
  ],
};
```

Verify each file path exists in docs/src/api/. This file matches the Docusaurus plugin configuration expecting module names without .md extension.
</action>
  <verify>ls docs/api-sidebar.js && node -e "const s = require('./docs/api-sidebar.js'); console.log('✓ Valid sidebar config with', s.apiSidebar.length, 'categories')"</verify>
  <done>api-sidebar.js created with 9 modules and 59 API file references</done>
</task>

<task type="auto">
  <name>Task 2: Enable API plugin in docusaurus.config.js</name>
  <files>docs/docusaurus.config.js</files>
  <action>Uncomment the API plugin configuration in docs/docusaurus.config.js:

1. Delete or uncomment lines 38-54 (the commented plugins array with API plugin)
2. Replace with active configuration:

```javascript
  plugins: [
    [
      '@docusaurus/plugin-content-docs',
      {
        id: 'api',
        path: 'src/api',
        routeBasePath: 'api',
        sidebarPath: 'api-sidebar.js',
        showLastUpdateAuthor: false,
        showLastUpdateTime: false,
      },
    ],
  ],
```

3. Remove the TODO comment (line 38-39: "API plugin temporarily disabled due to MDX syntax issues")

The API pages were already regenerated with escaped angle brackets (see 06-04 SUMMARY), so MDX syntax issues are resolved.
</action>
  <verify>grep -q "@docusaurus/plugin-content-docs" docs/docusaurus.config.js && ! grep -q "// TODO: API plugin temporarily disabled" docs/docusaurus.config.js</verify>
  <done>API plugin enabled in docusaurus.config.js with /api route and api-sidebar.js reference</done>
</task>

<task type="auto">
  <name>Task 3: Enable API sidebar in sidebars.js</name>
  <files>docs/sidebars.js</files>
  <action>Uncomment the apiSidebar configuration in docs/sidebars.js:

1. Delete or uncomment lines 22-54 (the commented apiSidebar array)
2. Add the active apiSidebar export by importing api-sidebar.js:

```javascript
const apiSidebarConfig = require('./api-sidebar.js');

module.exports = {
  guidesSidebar: [
    'intro',
    'getting-started',
    {
      type: 'category',
      label: 'Core Concepts',
      items: ['architecture', 'components'],
    },
    {
      type: 'category',
      label: 'Graphics',
      items: ['canvas', 'sprites', 'text-rendering'],
    },
    {
      type: 'category',
      label: 'Scenes',
      items: ['scene-management', 'scene-transitions'],
    },
  ],

  apiSidebar: apiSidebarConfig.apiSidebar,
};
```

3. Remove the TODO comment about re-enabling API sidebar

This imports the api-sidebar.js created in Task 1 and makes it available to Docusaurus.
</action>
  <verify>grep -q "apiSidebar:" docs/sidebars.js && ! grep -q "// TODO: Re-enable API sidebar" docs/sidebars.js</verify>
  <done>apiSidebar enabled in sidebars.js referencing api-sidebar.js configuration</done>
</task>

<task type="auto">
  <name>Task 4: Enable API navbar link in docusaurus.config.js</name>
  <files>docs/docusaurus.config.js</files>
  <action>Uncomment the API Reference navbar item in docs/docusaurus.config.js:

1. In the navbar.items array (around line 65), uncomment lines 72-80
2. Replace the commented block with:

```javascript
        {
          type: 'docSidebar',
          sidebarId: 'apiSidebar',
          position: 'left',
          label: 'API Reference',
        },
```

3. Remove the TODO comment (line 72: "Re-enable API Reference when API docs are fixed")

4. Also uncomment the footer link (around line 96):
```javascript
            { label: 'API Reference', to: '/api/core/Object' },
```

This adds the API Reference link to the navbar between "Guides" and "GitHub".
</action>
  <verify>grep -A2 "type: 'docSidebar'" docs/docusaurus.config.js | grep -q "sidebarId: 'apiSidebar'" && grep -q "API Reference" docs/docusaurus.config.js</verify>
  <done>API Reference link added to navbar (and footer) pointing to apiSidebar</done>
</task>

<task type="auto">
  <name>Task 5: Verify Docusaurus build succeeds with API navigation</name>
  <files>docs/build/</files>
  <action>Build the Docusaurus site to verify API navigation works:

1. cd docs && npm run build
2. Check that build completes without errors
3. Verify docs/build/api/ directory exists with generated HTML files
4. Check docs/build/sitemap.xml includes API pages

If build fails, it's likely due to:
- Invalid sidebar IDs (files don't exist) - fix api-sidebar.js
- MDX syntax errors in generated API files - regenerate with proper escaping

The API files were already generated with escaped angle brackets (from 06-04), so MDX should work.
</action>
  <verify>cd docs && npm run build && ls docs/build/api/core/Object.html && grep -q "docs/build/api/" docs/build/sitemap.xml</verify>
  <done>Docusaurus build succeeds, API pages exist in build output and sitemap</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete API reference navigation system (api-sidebar.js + enabled plugin + navbar link + sidebar navigation)</what-built>
  <how-to-verify>
    1. Start local server: cd docs && npm run serve
    2. Visit http://localhost:3000 (or the URL shown)
    3. Verify navbar shows "Guides" and "API Reference" links
    4. Click "API Reference" - should show dropdown or sidebar with 9 modules
    5. Click a module (e.g., "Core") - should expand showing class list
    6. Click a class (e.g., "Object") - should navigate to API page
    7. Verify left sidebar shows module structure with active page highlighted
    8. Try a few different modules (Graphics, Utils, Scripting) to confirm navigation works

    Expected behavior:
    - Navbar has both "Guides" and "API Reference" links
    - API Reference shows module categories (Abstract, Animation, Components, Core, Effects, Graphics, Scripting, UI, Utils)
    - Each module expands to show list of classes
    - Clicking a class navigates to /api/{module}/{Class} page
    - API pages have left sidebar with module navigation, active class highlighted
  </how-to-verify>
  <resume-signal>Type "approved" if navigation works, or describe any issues (missing sidebar, broken links, build errors)</resume-signal>
</task>

</tasks>

<verification>
- API plugin enabled in docusaurus.config.js
- api-sidebar.js exists with 9 modules and 59 API files
- apiSidebar enabled in sidebars.js
- API navbar link present and functional
- Docusaurus builds without errors
- API pages have left sidebar navigation
- Navbar "API Reference" link shows module structure
</verification>

<success_criteria>
API reference accessible through Docusaurus site navigation with:
- Working navbar "API Reference" link
- Module-based sidebar (9 categories)
- All 59 API pages reachable via navigation
- Left sidebar shows active module and page
</success_criteria>

<output>
After completion, create `.planning/phases/06-create-library-docs-using-doxygen-docusaurus/06-06-SUMMARY.md`
</output>
