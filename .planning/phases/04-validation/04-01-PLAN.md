---
phase: 04-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - enjin2/vendor/stb_image_write.h
  - enjin2/include/enjin2/graphics/canvas.hpp
  - enjin2/src/graphics/canvas.cpp
autonomous: true

must_haves:
  truths:
    - stb_image_write library integrated and available for BMP export
    - Canvas class provides exportToBMP() method for image output
    - BMP export produces valid 24-bit RGB images viewable in standard image viewers
  artifacts:
    - path: enjin2/vendor/stb_image_write.h
      provides: Single-header BMP export library
      min_lines: 1
    - path: enjin2/include/enjin2/graphics/canvas.hpp
      provides: exportToBMP() method declaration
      contains: "void exportToBMP"
    - path: enjin2/src/graphics/canvas.cpp
      provides: BMP export implementation
      contains: "STB_IMAGE_WRITE_IMPLEMENTATION"
  key_links:
    - from: enjin2/src/graphics/canvas.cpp
      to: enjin2/vendor/stb_image_write.h
      via: #define STB_IMAGE_WRITE_IMPLEMENTATION
      pattern: "#define STB_IMAGE_WRITE_IMPLEMENTATION"
---

<objective>
Add BMP image export capability to Canvas using stb_image_write library. This enables test artifacts (rendered output) to be saved as BMP images for manual inspection and automated comparison.

Purpose: BMP format required for broad compatibility (unlike PGM which has limited support). BMP images serve as test artifacts for manual verification and shadow mode comparison.
Output: Canvas::exportToBMP() method that writes 24-bit RGB BMP files, stb_image_write integrated.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md
@enjin2/include/enjin2/graphics/canvas.hpp
@enjin2/src/graphics/canvas.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate stb_image_write library</name>
  <files>enjin2/vendor/stb_image_write.h</files>
  <action>
    Download stb_image_write.h from https://github.com/nothings/stb and save to enjin2/vendor/stb_image_write.h. Create vendor directory if it doesn't exist. Do NOT modify the file - it's a single-header library with public domain license. Reference: https://github.com/nothings/stb/blob/master/stb_image_write.h
  </action>
  <verify>
    File exists and contains "stb_image_write.h" header with license comment
  </verify>
  <done>
    stb_image_write.h present in enjin2/vendor/ directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Add BMP export method to Canvas</name>
  <files>enjin2/include/enjin2/graphics/canvas.hpp, enjin2/src/graphics/canvas.cpp</files>
  <action>
    In canvas.hpp: Add void exportToBMP(const char *filename) const method declaration next to exportToPGM() method.

    In canvas.cpp: At top of file after existing includes, add:
    #define STB_IMAGE_WRITE_IMPLEMENTATION
    #include "../vendor/stb_image_write.h"

    Implement exportToBMP() method:
    - Convert internal pixel format to 24-bit RGB (gray=R=G=B) for compatibility
    - Use stbi_write_bmp() function from stb_image_write
    - Follow same error handling pattern as exportToPGM() (check filename, return silently on error)
    - Add comment: "Export canvas to BMP format (24-bit RGB, grayscale as gray=R=G=B)"

    Use existing exportToPGM() implementation as reference for structure and error handling.
  </action>
  <verify>
    grep -n "exportToBMP" enjin2/include/enjin2/graphics/canvas.hpp shows method declaration
    grep -n "stbi_write_bmp" enjin2/src/graphics/canvas.cpp shows usage
    Compile succeeds with no errors
  </verify>
  <done>
    Canvas class has exportToBMP() method that writes valid BMP files
  </done>
</task>

</tasks>

<verification>
1. stb_image_write.h exists in enjin2/vendor/ directory
2. exportToBMP() method declared in canvas.hpp
3. exportToBMP() method implemented in canvas.cpp with STB_IMAGE_WRITE_IMPLEMENTATION
4. Project compiles successfully with no errors
5. Run existing example (e.g., basic_drawing) modified to call exportToBMP() - verify BMP file created and opens in system image viewer
</verification>

<success_criteria>
- stb_image_write library integrated (no build errors)
- Canvas::exportToBMP() method available
- BMP files export correctly (24-bit RGB, grayscale compatible)
- No regression in existing PGM export functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-01-SUMMARY.md`
</output>
