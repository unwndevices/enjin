---
phase: 04-validation
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - .planning/phases/04-validation/format_results.sh
autonomous: true

must_haves:
  truths:
    - Test results formatted with summary output and BMP image references
    - Minimal failure presentation (test name + error code + BMP reference)
    - Results displayed chronologically in execution order
    - Clear separation between passed and failed tests
  artifacts:
    - path: .planning/phases/04-validation/format_results.sh
      provides: Terminal output formatter
      min_lines: 60
  key_links:
    - from: .planning/phases/04-validation/format_results.sh
      to: test-results-manual-* directories
      via: Read summary.md files
      pattern: "summary.md"
    - from: .planning/phases/04-validation/format_results.sh
      to: test-results-shadow-* directories
      via: Read comparison.txt files
      pattern: "comparison.txt"
---

<objective>
Create terminal output formatter for test results that displays summaries with BMP image references. Results show minimal failure information (name, error, BMP reference) and are displayed chronologically.

Purpose: Provide clear, concise test results output that references BMP images for detailed inspection. Minimal presentation keeps output focused.
Output: format_results.sh script that formats manual and shadow mode test results for terminal display.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md
@.planning/phases/04-validation/04-02-PLAN.md
@.planning/phases/04-validation/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task: Create test result formatter</name>
  <files>.planning/phases/04-validation/format_results.sh</files>
  <action>
    Create shell script format_results.sh with:
    - Shebang: #!/bin/bash
    - Functions: format_manual_results(), format_shadow_results(), print_summary()

    format_manual_results():
      - Find all test-results-manual-* directories sorted by timestamp
      - For each directory, read summary.md file
      - Extract test results (Test name, status, BMP file)
      - Format output chronologically:
        ```
        Manual Test Results (2026-01-31 14:30:15)
        ✓ Test 1: Awake Order
        ✓ Test 2: Start Order
        ✗ Test 3: Update Execution (see test-03/output.bmp)
        ...
        Summary: 7/8 passed
        ```
      - Show BMP path for failed tests only

    format_shadow_results():
      - Find all test-results-shadow-* directories sorted by timestamp
      - For each directory, read comparison.txt files
      - Format output chronologically:
        ```
        Shadow Mode Results (2026-01-31 14:35:20)
        Test 1: Basic Rendering
          Pixel difference: 1.2% (within 3% tolerance)
          Timing: enjin1=45ms, enjin2=48ms (6.7% gap, no warning)
        Test 2: Scene Transition
          Pixel difference: 4.5% (⚠️ EXCEEDS 3% tolerance)
          Timing: enjin1=120ms, enjin2=150ms (25% gap, ⚠️ WARNING)
          See: test-02/enjin1-output.bmp, test-02/enjin2-output.bmp
        ...
        Summary: 4/5 tests within tolerance, 2 timing warnings
        ```
      - Use symbols: ✓ for pass, ✗ for fail, ⚠️ for warning

    print_summary():
      - Aggregate results from all manual and shadow runs
      - Display overall stats: "Total Manual: 7/8 passed, Total Shadow: 4/5 within tolerance"
      - List failed tests with BMP references

    Script should be executable: chmod +x format_results.sh
  </action>
  <verify>
    Script exists with all three functions
    Script reads summary.md from manual test directories
    Script reads comparison.txt from shadow test directories
    Script outputs formatted results chronologically
    Failed tests show BMP references
  </verify>
  <done>
    Formatter script displays test results with BMP references, chronological order, minimal failure info
  </done>
</task>

</tasks>

<verification>
1. format_results.sh exists and is executable
2. Running format_results.sh reads manual and shadow test result directories
3. Manual test output shows test names, statuses, and BMP references for failures
4. Shadow test output shows pixel differences, timing info, and BMP references for warnings
5. Results displayed chronologically
6. Summary section aggregates all results
</verification>

<success_criteria>
- Terminal output formats manual test results (pass/fail, BMP refs for failures)
- Terminal output formats shadow mode results (pixel diff %, timing warnings, BMP refs)
- Results displayed chronologically (oldest to newest)
- Minimal failure presentation (just essentials, let BMP show details)
- Summary aggregates results from all test runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-04-SUMMARY.md`
</output>
