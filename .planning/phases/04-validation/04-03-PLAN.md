---
phase: 04-validation
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - .planning/phases/04-validation/shadow-test.sh
  - enjin2/tests/shadow_mode_test.cpp
autonomous: true

must_haves:
  truths:
    - Shadow mode builds both enjin1 and enjin2 backends
    - Shadow mode executes both engines with identical input and captures output buffers
    - Shadow mode exports both outputs as BMP images and runs comparison
    - Shadow mode warns on significant timing gaps (>20% or >50ms)
    - Shadow mode summarizes all differences (not fail-fast)
  artifacts:
    - path: .planning/phases/04-validation/shadow-test.sh
      provides: Shadow mode execution script
      min_lines: 80
    - path: enjin2/tests/shadow_mode_test.cpp
      provides: Shadow mode test executable
      min_lines: 80
  key_links:
    - from: .planning/phases/04-validation/shadow-test.sh
      to: enjin2/CMakeLists.txt
      via: cmake -DUSE_ENJIN1=ON/OFF
      pattern: "cmake -DUSE_ENJIN1"
    - from: .planning/phases/04-validation/shadow-test.sh
      to: enjin2/tests/image_comparison_test
      via: Run comparison executable
      pattern: "./image_comparison_test"
    - from: enjin2/tests/shadow_mode_test.cpp
      to: enjin2/include/enjin2/graphics/canvas.hpp
      via: exportToBMP()
      pattern: "exportToBMP"
---

<objective>
Implement shadow mode execution that runs both enjin1 and enjin2 engines in parallel, captures output buffers, compares them, and reports behavioral differences. Shadow mode enables automated verification of enjin2 equivalence to enjin1.

Purpose: Validate enjin2 behavior matches enjin1 through parallel execution with output comparison. Detect regressions and behavioral deviations early.
Output: Shadow mode execution script, shadow mode test executable, comparison reports with pixel differences and timing warnings.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md
@.planning/phases/04-validation/04-01-PLAN.md
@.planning/phases/04-validation/04-02-PLAN.md
@enjin2/tests/image_comparison.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shadow mode test executable</name>
  <files>enjin2/tests/shadow_mode_test.cpp</files>
  <action>
    Create shadow_mode_test.cpp with:
    - Main function takes backend type as argument (enjin1 or enjin2)
    - Create standard test scene with: 3 objects with C_Position, C_Drawable components
    - Run scene for fixed number of frames (e.g., 60 frames)
    - Measure execution time using std::chrono (high_resolution_clock)
    - After test frame count, call canvas->exportToBMP("output-enjin1.bmp" or "output-enjin2.bmp")
    - Output timing to stdout: "Execution time: XXX ms"
    - Return 0 on success, 1 on error

    Ensure identical input (same scene config, same object positions, same frame count).

    Update CMakeLists.txt to build shadow_mode_test_enjin1 and shadow_mode_test_enjin2 executables using USE_ENJIN1 option.
  </action>
  <verify>
    enjin2/tests/shadow_mode_test.cpp exists with main() function
    CMakeLists.txt updated to build both test executables
    Compiles with no errors
  </verify>
  <done>
    Shadow mode test executable builds for both enjin1 and enjin2 backends
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shadow mode execution script</name>
  <files>.planning/phases/04-validation/shadow-test.sh</files>
  <action>
    Create shell script shadow-test.sh with:
    - Shebang: #!/bin/bash
    - Initialize results directory: test-results-shadow-$(date +%Y%m%d-%H%M%S)/
    - Functions: build_backends(), run_shadow_tests(), compare_outputs(), report_results()

    build_backends():
      - Build enjin1 backend: cmake -DUSE_ENJIN1=ON, make shadow_mode_test_enjin1
      - Build enjin2 backend: cmake -DUSE_ENJIN1=OFF, make shadow_mode_test_enjin2
      - Report build success/failure

    run_shadow_tests():
      - Run enjin1 test, capture timing
      - Run enjin2 test, capture timing
      - Export both outputs as BMP (test executable does this)
      - Move BMPs to results directory

    compare_outputs():
      - Run image_comparison_test on the two BMP files
      - Capture pixel difference percentage
      - Check if >3% tolerance
      - Compare timing: if gap >20% or >50ms absolute, issue warning

    report_results():
      - Print summary to terminal
      - Format: "Shadow Mode Summary: X differences found (N >3% tolerance), Y timing warnings"
      - List each failing test with BMP references
      - Continue all tests (not fail-fast), summarize at end

    Script should be executable: chmod +x shadow-test.sh
  </action>
  <verify>
    Script exists with all four functions
    Script builds both backends with correct CMake options
    Script runs comparison and checks 3% tolerance
    Script warns on timing gaps >20% or >50ms
  </verify>
  <done>
    Shadow mode script executes both backends, compares outputs, reports differences and warnings
  </done>
</task>

</tasks>

<verification>
1. shadow_mode_test.cpp compiles for both enjin1 and enjin2 backends
2. shadow-test.sh builds both backends correctly
3. Running shadow-test.sh executes both engines
4. BMP files are exported and compared
5. Summary report shows pixel differences and timing warnings
6. Script does NOT fail-fast - runs all tests and summarizes
</verification>

<success_criteria>
- Shadow mode builds both enjin1 and enjin2 backends
- Identical input provided to both engines (same scene config, same frame count)
- Output buffers captured and exported as BMP
- Pixel comparison runs with 3% tolerance threshold
- Timing gaps detected (>20% or >50ms) and reported as warnings
- Summary report lists all differences and references BMP files
- Execution continues through all tests (no fail-fast)
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-03-SUMMARY.md`
</output>
