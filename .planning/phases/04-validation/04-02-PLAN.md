---
phase: 04-validation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - enjin2/tests/image_comparison.cpp
  - .planning/phases/04-validation/manual-test-checklist.md
  - .planning/phases/04-validation/manual-test.sh
autonomous: true

must_haves:
  truths:
    - Image comparison utility calculates pixel difference percentage between two BMP images
    - Manual testing checklist covers lifecycle, rendering, transitions, and Lua scenarios
    - Manual test script executes checklist items chronologically and saves artifacts
  artifacts:
    - path: enjin2/tests/image_comparison.cpp
      provides: Image comparison utility
      min_lines: 50
    - path: .planning/phases/04-validation/manual-test-checklist.md
      provides: Test scenarios for manual verification
      contains: "Test 1:", "Test 2:"
    - path: .planning/phases/04-validation/manual-test.sh
      provides: Manual test execution script
      min_lines: 50
  key_links:
    - from: enjin2/tests/image_comparison.cpp
      to: enjin2/vendor/stb_image_write.h
      via: stbi_load for reading BMP files
      pattern: "stbi_load"
    - from: .planning/phases/04-validation/manual-test.sh
      to: .planning/phases/04-validation/manual-test-checklist.md
      via: Source markdown checklist
      pattern: "manual-test-checklist.md"
---

<objective>
Create image comparison utility and manual testing infrastructure. Image comparison calculates pixel differences between enjin1 and enjin2 output for shadow mode verification. Manual testing provides checklist-based approach for critical path verification.

Purpose: Support both automated comparison (shadow mode) and human verification (manual testing) of engine behavior.
Output: Image comparison C++ utility, manual test checklist, manual test execution script.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md
@.planning/phases/04-validation/04-01-PLAN.md
@enjin2/vendor/stb_image_write.h
@enjin2/tests/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create image comparison utility</name>
  <files>enjin2/tests/image_comparison.cpp</files>
  <action>
    Create enjin2/tests/image_comparison.cpp with:
    - Function: float compareBMP(const char* file1, const char* file2) that returns pixel difference percentage (0.0 to 100.0)
    - Use stbi_load() from stb_image (add stb_image.h to vendor directory or use stb_image_write's built-in loader)
    - Load both BMP files, verify dimensions match
    - Loop through pixels, count differences (R, G, B channels)
    - Calculate: (different_pixels / total_pixels) * 100.0
    - Main function takes 2 file paths as arguments, outputs difference percentage to stdout
    - Return 0 if difference <= 3.0%, return 1 otherwise

    Update CMakeLists.txt in enjin2/ to build image_comparison_test executable.

    Use stb_image_write.h - it includes stb_image.h for loading.
  </action>
  <verify>
    enjin2/tests/image_comparison.cpp exists with compareBMP() function
    CMakeLists.txt updated to build image_comparison_test executable
    Compiling works
  </verify>
  <done>
    Image comparison utility compiles and outputs pixel difference percentage
  </done>
</task>

<task type="auto">
  <name>Task 2: Create manual testing checklist</name>
  <files>.planning/phases/04-validation/manual-test-checklist.md</files>
  <action>
    Create manual-test-checklist.md with sections:

    ## Component Lifecycle Tests
    ### Test 1: Awake Order Verification
    - Objective: Verify Awake() methods execute in correct order when objects are added to scene
    - Test: Add 3 objects with components that log Awake execution, verify order matches scene hierarchy
    - Expected: Components log Awake() in scene graph order

    ### Test 2: Start Order Verification
    - Objective: Verify Start() methods execute after all Awake() calls complete
    - Test: Add objects, wait for scene activation, verify Start() calls after Awake()
    - Expected: Start() runs once per scene activation

    ### Test 3: Update Execution
    - Objective: Verify Update() method executes on each frame
    - Test: Create component with frame counter, run scene for 60 frames, verify counter increments
    - Expected: Update() called each frame with deltaTime

    ## Rendering Tests
    ### Test 4: Basic Shape Rendering
    - Objective: Verify basic shapes (rectangle, circle, line) render correctly
    - Test: Draw rectangle, circle, line on canvas, export to BMP, inspect visually
    - Expected: Shapes appear at correct positions with correct colors

    ### Test 5: Layer Ordering
    - Objective: Verify draw layer (z-order) affects rendering order
    - Test: Create 3 overlapping objects with different layers, export BMP, verify layering
    - Expected: Higher layer numbers draw on top

    ## Scene Transition Tests
    ### Test 6: Scene Push Behavior
    - Objective: Verify scene push preserves current scene
    - Test: Push scene on top of active scene, verify bottom scene still exists
    - Expected: Two scenes in stack, top scene is active

    ### Test 7: Scene Pop Behavior
    - Objective: Verify scene pop destroys top scene and restores previous
    - Test: Pop scene from stack, verify previous scene becomes active
    - Expected: Top scene destroyed, previous scene active

    ## Lua Scripting Tests
    ### Test 8: Basic Script Execution
    - Objective: Verify Lua scripts can be loaded and executed
    - Test: Create Lua script with print statement, run via LuaEngine, verify output
    - Expected: Script executes and prints to console

    Each test section has: Objective, Test, Expected
  </action>
  <verify>
    File exists with all 8 test scenarios
    Each test has Objective, Test, Expected fields
  </verify>
  <done>
    Manual testing checklist covers all critical paths (lifecycle, rendering, transitions, Lua)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create manual test execution script</name>
  <files>.planning/phases/04-validation/manual-test.sh</files>
  <action>
    Create shell script manual-test.sh with:
    - Shebang: #!/bin/bash
    - Initialize test results directory: test-results-manual-$(date +%Y%m%d-%H%M%S)/
    - Create subdirectories for each test scenario
    - Functions: init_tests(), run_test_scenario(), export_output(), summarize_results()
    - Read manual-test-checklist.md, extract test scenarios
    - Execute each scenario chronologically (Test 1, Test 2, etc.)
    - For each test: run, export canvas output to BMP, save to timestamped subdirectory
    - Output format to terminal: "Test 1: Awake Order - PASSED/FAILED (output.bmp)"
    - Create summary.md in results directory with overall pass/fail and BMP references
    - Exit code: 0 if all tests pass, 1 if any fail

    Script should be executable: chmod +x manual-test.sh
  </action>
  <verify>
    Script exists with proper shebang and functions
    Script reads manual-test-checklist.md
    Script creates timestamped result directories
  </verify>
  <done>
    Manual test script executes checklist items, saves BMP artifacts, outputs summary
  </done>
</task>

</tasks>

<verification>
1. Image comparison utility builds successfully
2. Manual testing checklist has 8 test scenarios covering all areas
3. Manual test script is executable and can read checklist
4. Running manual-test.sh creates timestamped directory structure
5. Test output shows BMP file references
</verification>

<success_criteria>
- Image comparison utility calculates pixel difference percentage
- Manual testing checklist covers lifecycle, rendering, transitions, and Lua
- Manual test script executes chronologically with artifact collection
- All components work together (comparison + checklist + script)
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-02-SUMMARY.md`
</output>
