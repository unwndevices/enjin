---
phase: 16-repository-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/enjin2/compat/component.hpp
  - include/enjin2/compat/module_group.hpp
  - include/enjin2/compat/scene.hpp
  - include/enjin2/compat/types.hpp
  - examples/enjin_comparison_benchmark.cpp
  - examples/eisei_game_benchmark.cpp
  - examples/CMakeLists.txt
  - docs/api/compat/README.md
  - docs/api/compat/Vector3.md
  - docs/api/README.md
autonomous: true
requirements:
  - DEAD-01
  - DEAD-02
  - DEAD-03
  - DEAD-04

must_haves:
  truths:
    - "include/enjin2/compat/ directory does not exist in the repository"
    - "enjin_comparison_benchmark.cpp does not exist in examples/"
    - "eisei_game_benchmark.cpp does not exist in examples/"
    - "CMake configuration succeeds with no references to removed files"
    - "No source file contains #include referencing compat/"
  artifacts:
    - path: "examples/CMakeLists.txt"
      provides: "Clean CMake config with no commented-out enjin1 benchmark targets"
  key_links:
    - from: "examples/CMakeLists.txt"
      to: "examples/*.cpp"
      via: "add_executable targets"
      pattern: "add_executable.*benchmark"
---

<objective>
Remove all dead enjin1 remnants: compat headers, dead benchmark files, commented-out CMake targets, and orphaned API docs.

Purpose: Eliminate enjin1 artifacts so the codebase contains only live enjin2 code.
Output: Clean repository with no compat/ directory, no dead benchmarks, no broken references.
</objective>

<execution_context>
@/home/unwn/.claude/get-shit-done/workflows/execute-plan.md
@/home/unwn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete compat headers and dead benchmark files</name>
  <files>
    include/enjin2/compat/component.hpp
    include/enjin2/compat/module_group.hpp
    include/enjin2/compat/scene.hpp
    include/enjin2/compat/types.hpp
    examples/enjin_comparison_benchmark.cpp
    examples/eisei_game_benchmark.cpp
    docs/api/compat/README.md
    docs/api/compat/Vector3.md
  </files>
  <action>
    Delete the entire `include/enjin2/compat/` directory (4 files: component.hpp, module_group.hpp, scene.hpp, types.hpp).

    Delete the two dead benchmark files from `examples/`:
    - `examples/enjin_comparison_benchmark.cpp`
    - `examples/eisei_game_benchmark.cpp`

    Delete the orphaned API doc directory `docs/api/compat/` (contains README.md and Vector3.md — these document the compat headers being removed).

    Use `git rm -r` for all deletions so they are staged for commit.

    Commands:
    ```bash
    git rm -r include/enjin2/compat/
    git rm examples/enjin_comparison_benchmark.cpp examples/eisei_game_benchmark.cpp
    git rm -r docs/api/compat/
    ```
  </action>
  <verify>
    <automated>test ! -d include/enjin2/compat/ && test ! -f examples/enjin_comparison_benchmark.cpp && test ! -f examples/eisei_game_benchmark.cpp && test ! -d docs/api/compat/ && echo "PASS"</automated>
  </verify>
  <done>compat/ directory, both dead benchmark files, and orphaned compat API docs no longer exist in the working tree</done>
</task>

<task type="auto">
  <name>Task 2: Clean up CMake and documentation references to removed files</name>
  <files>
    examples/CMakeLists.txt
    docs/api/README.md
  </files>
  <action>
    In `examples/CMakeLists.txt`, remove the two commented-out target blocks entirely:

    1. Remove lines 14-21 (the "Enjin vs Enjin2 comparison benchmark" block — comment header + all commented-out cmake commands for enjin_comparison_benchmark).

    2. Remove lines 23-32 (the "Eisei Game benchmark" block — comment header + all commented-out cmake commands for eisei_game_benchmark).

    In `docs/api/README.md`, remove the line referencing the compat API docs:
    - Remove the line: `- [Compat](./compat/) - Compatibility layer for enjin1 integration`

    After editing, verify the CMake configuration succeeds:
    ```bash
    cd /home/unwn/dev/enjin && cmake -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON 2>&1 | tail -5
    ```

    Grep the entire project for any remaining references to compat/ headers or the deleted benchmark files. If any are found in source code (not INTEGRATION_REPORT.md or planning docs), fix them. INTEGRATION_REPORT.md is a historical artifact and should be left as-is.
  </action>
  <verify>
    <automated>cd /home/unwn/dev/enjin && cmake -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON 2>&1 | grep -i error; test $? -eq 1 && echo "PASS: No CMake errors"</automated>
  </verify>
  <done>CMake configures successfully. No commented-out enjin1 benchmark targets remain. No broken references to removed files in active code.</done>
</task>

</tasks>

<verification>
1. `include/enjin2/compat/` directory does not exist
2. `examples/enjin_comparison_benchmark.cpp` does not exist
3. `examples/eisei_game_benchmark.cpp` does not exist
4. `docs/api/compat/` directory does not exist
5. `examples/CMakeLists.txt` has no commented-out enjin1 targets
6. `docs/api/README.md` has no compat link
7. `cmake -B build` succeeds without errors
8. `grep -r "enjin2/compat/" include/ src/ examples/` returns no results
</verification>

<success_criteria>
- Zero compat/ files in repository
- Zero dead benchmark files in examples/
- CMake configuration succeeds
- No broken includes or missing targets
</success_criteria>

<output>
After completion, create `.planning/phases/16-repository-cleanup/16-01-SUMMARY.md`
</output>
