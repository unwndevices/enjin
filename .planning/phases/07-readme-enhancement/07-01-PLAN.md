---
phase: 07-readme-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User visiting repository can understand enjin2's purpose and capabilities from README"
    - "User can see list of key features (static allocation, Lua/WASM, multi-platform)"
    - "User can navigate to API documentation, guides, and GitHub Pages from README"
  artifacts:
    - path: "README.md"
      provides: "Professional project entry point"
      min_lines: 80
      max_lines: 120
  key_links:
    - from: "README.md"
      to: "https://unwndevices.github.io/enjin/"
      via: "Documentation links section"
      pattern: "https://unwndevices.github.io/enjin/"
---

<objective>
Create professional README with clear project description, key features list, and navigation links to documentation.

Purpose: enjin2 needs a compelling README that helps visitors quickly understand the library's purpose and unique capabilities (static allocation, Lua/WASM integration, multi-platform support), then navigate to comprehensive documentation.
Output: Updated README.md following established C++ library patterns with badges, description, features, installation, quick start, and documentation links.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-readme-enhancement/07-CONTEXT.md
@.planning/phases/07-readme-enhancement/07-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add badges, title, tagline, description, and features list</name>
  <files>README.md</files>
  <action>
    Update README.md with:
    
    1. **Badges row** at top (3 badges):
       - CI badge: `![CI](https://img.shields.io/github/actions/workflow/status/unwndevices/enjin/docs.yml?branch=main)`
       - Docs badge: `![Docs](https://img.shields.io/badge/docs-latest-blue)`
       - License placeholder: `![License](https://img.shields.io/badge/license-TBD-green)`
    
    2. **Title and tagline** (replace current title line):
       - `# enjin2 - Lightweight C++ game engine for embedded devices`
    
    3. **Description paragraph** (new section after title):
       - Write 2-3 sentences explaining enjin2's purpose as a self-contained C++ game engine library with static allocation (no dynamic memory), Lua/WASM integration for game logic scripting, and multi-platform support (ESP32-S3 and desktop). Target audience is expert C++ developers - no embedded/C++ concept explanations.
    
    4. **Features list** (enhance existing Features section):
       Keep existing features but add:
       - **Static Allocation:** No dynamic memory usage, predictable performance
       - **Lua/WASM Integration:** Script game logic, target web platforms
       - **Multi-Platform:** ESP32-S3 and desktop support
    
    Total features should be 5-7 items, with the 3 key differentiators (static allocation, Lua/WASM, multi-platform) at the top.
    
    Use the existing README structure as starting point. Don't completely rewrite - enhance what's there.
  </action>
  <verify>
    grep -q "![CI](https://img.shields.io/github/actions/workflow/status" README.md
    grep -q "![Docs](https://img.shields.io/badge/docs-latest-blue)" README.md
    grep -q "# enjin2 - Lightweight C++ game engine" README.md
    grep -q "Static Allocation" README.md
    grep -q "Lua/WASM Integration" README.md
    grep -q "Multi-Platform" README.md
  </verify>
  <done>
    Badges row at top showing CI status, docs, and license
    Title includes tagline "Lightweight C++ game engine for embedded devices"
    Description paragraph explains enjin2's purpose (2-3 sentences)
    Features list has 5-7 items with static allocation, Lua/WASM, multi-platform at top
  </done>
</task>

<task type="auto">
  <name>Task 2: Add installation, quick start, and documentation links sections</name>
  <files>README.md</files>
  <action>
    Add/update these sections in README.md:
    
    1. **Installation** (enhance existing Quick Start > Building section):
       - Rename "Quick Start > Building" to "Installation"
       - Keep cmake build commands but add git clone step at top:
         ```bash
         git clone https://github.com/unwndevices/enjin.git
         cd enjin
         mkdir build && cd build
         cmake ..
         cmake --build .
         ```
    
    2. **Quick Start** (new section after Installation):
       - Add minimal working example (5-8 lines):
         ```cpp
         #include <enjin2.hpp>
         using namespace enjin2;
         
         int main() {
             Canvas8_128x64 canvas;
             canvas.fillRect(10, 10, 108, 44, 15);
             return 0;
         }
         ```
       - Brief description: "Creates a canvas and draws a filled rectangle"
    
    3. **Documentation** (relocate and enhance existing Documentation section):
       - Move to before Project Structure (better flow)
       - Add "Full documentation available at: https://unwndevices.github.io/enjin/"
       - Keep existing links to Getting Started, API Reference, Architecture
       - Add any missing links from docs site (check docs/src/ for available guides)
    
    4. **License** (update existing License section):
       - Keep existing placeholder "[Add license information]" since no LICENSE file exists
    
    Ensure total README length stays under 120 lines (current is 51, additions should be concise).
  </action>
  <verify>
    grep -q "## Installation" README.md
    grep -q "git clone https://github.com/unwndevices/enjin.git" README.md
    grep -q "## Quick Start" README.md
    grep -q "Canvas8_128x64 canvas" README.md
    grep -q "## Documentation" README.md
    grep -q "https://unwndevices.github.io/enjin/" README.md
    wc -l README.md | awk '{print $1}' | grep -q "^[0-9][0-9]*$"
  </verify>
  <done>
    Installation section includes git clone + cmake build steps
    Quick Start section has minimal working example (5-8 lines of C++)
    Documentation section moved before Project Structure with full URL + guide links
    Total README length is under 120 lines
  </done>
</task>

</tasks>

<verification>
After completing both tasks, verify README.md content with automated checks:
1. Verify docs link is reachable: curl -Is https://unwndevices.github.io/enjin/ | head -1
2. Verify all badge URLs are present: grep -E 'img\.shields\.io' README.md | wc -l | grep -q '3'
3. Verify code blocks have language specifiers: grep -E '```(cpp|bash|md)' README.md | wc -l | grep -q '[3-9]'
4. Verify section headers present: grep -E '^## ' README.md | wc -l | grep -q '[5-9]'
5. Verify README is scannable (not wall of text): awk 'NF<3 && length>150' README.md | wc -l | grep -q '0'
</verification>

<success_criteria>
README.md provides clear project description with tagline and 2-3 sentence explanation
README.md lists 5-7 key features with static allocation, Lua/WASM, multi-platform prominently featured
README.md includes working badges for CI, docs, and license
README.md has installation instructions (git clone + cmake build)
README.md has quick start example (minimal working C++ code)
README.md has documentation links section with full docs URL and specific guide links
README.md is under 120 lines total (concise, not bloated)
README.md renders correctly on GitHub with proper formatting and working links
</success_criteria>

<output>
After completion, create `.planning/phases/07-readme-enhancement/07-01-SUMMARY.md`
</output>
