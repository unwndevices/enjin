#include "../../include/enjin2/ui/theme.hpp"

namespace enjin2 {

// Theme is a header-only constexpr aggregate (see ui/theme.hpp); this translation
// unit exists so the header is compiled as part of enjin2_ui.

} // namespace enjin2